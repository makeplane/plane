"use client";

import { observer } from "mobx-react";
// plane web components
import TeamsListRoot from "@/plane-web/components/teams/list/root";

const TeamsListPage = observer(() => <TeamsListRoot />);

export default TeamsListPage;
