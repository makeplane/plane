apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api
  labels:
    component: api
    release: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    component: api
    release: {{ .Release.Name }}

