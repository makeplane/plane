import { FC, ReactNode } from "react";

type TInstanceSignInForm = {
  children: ReactNode;
};

export const InstanceSignInForm: FC<TInstanceSignInForm> = (props) => {
  const {} = props;

  return <div>SignIn Form</div>;
};
