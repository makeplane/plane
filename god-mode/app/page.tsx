"use client";

import { observer } from "mobx-react";
// components
import { PageHeader } from "@/components/core";

const RootPage = observer(() => (
  <>
    <PageHeader title="General Settings - God Mode" />
    Hello this is sainath
  </>
));

export default RootPage;
