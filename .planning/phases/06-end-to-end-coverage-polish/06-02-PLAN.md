---
plan_id: 06-02
phase: 6
wave: 2
depends_on: [06-01]
requirements: [TEST-23, TEST-24]
files_modified:
  - apps/web/e2e/specs/timeline-dependency-propagation.spec.ts
  - apps/web/e2e/README.md
files_created: []
autonomous: true
estimated_duration: 25m
must_haves:
  truths:
    - "TEST-23: drag predecessor `src` rightward by +4 calendar days → server 200 + `total_updated_count >= 2` + `requested_work_item_id === src.id`"
    - "TEST-23: DOM — `tgt` block bounding box shifts rightward by ≥ `dayWidth - 2px` (boundary violation forces tgt forward)"
    - "TEST-23: persistence — `api.getIssue(tgt.id)` returns `start_date` / `target_date` matching the server response's `work_items[].id === tgt.id` entry"
    - 'TEST-24: setup — `api.clearIssueDate(tgt.id, "target_date")` runs AFTER `propagationTimeline.gotoIssueGantt` + `waitForBlock(both)` so the browser local store retains stale populated tgt (D-07a)'
    - 'TEST-24: drag predecessor → server 422 + `body.code === "INCOMPLETE_SCHEDULE"`'
    - 'TEST-24: toast text — `getByText("Schedule update failed")` AND `getByText("A dependent work item is missing start or target dates.")` both visible'
    - "TEST-24: DOM rollback — both `src` and `tgt` block bounding boxes return to within ±2px of pre-drag positions (D-04c)"
    - "Existing 3 relation-creation specs (`#1`/`#2`/`#3` in `timeline-dependency-drag.spec.ts`) still pass — regression guard"
    - "All 4 Wave 1 smoke specs converted to `test.skip(...)` or removed (no duplication of seeded relation/drag flows; TEST-23/TEST-24 cover the same surface end-to-end)"
    - "`pnpm --filter=web test:e2e` passes the full suite TWICE consecutively — idempotency check (D-07 cleanup)"
    - "OxLint budget 11957 unchanged; TypeScript clean"
    - "`apps/web/e2e/README.md` includes a one-line locale precondition note (D-08a)"
  artifacts:
    - path: "apps/web/e2e/specs/timeline-dependency-propagation.spec.ts"
      provides: "TEST-23 + TEST-24 fully implemented; smoke tests skipped or removed; describe block contains exactly 2 active tests"
      contains: "TEST-23"
    - path: "apps/web/e2e/README.md"
      provides: "Locale precondition note for non-English workspaces"
      contains: "en"
  key_links:
    - from: "apps/web/e2e/specs/timeline-dependency-propagation.spec.ts (TEST-23)"
      to: "apps/web/e2e/fixtures/test-fixtures.ts (propagationTimeline + propagationPair)"
      via: "fixture destructuring"
      pattern: "propagationTimeline.*propagationPair"
    - from: "apps/web/e2e/specs/timeline-dependency-propagation.spec.ts (TEST-23 network assertion)"
      to: "apps/api/plane/app/views/issue/timeline_propagation.py (200 success path)"
      via: "page.waitForResponse filter on /timeline-propagation/"
      pattern: "/timeline-propagation/"
    - from: "apps/web/e2e/specs/timeline-dependency-propagation.spec.ts (TEST-23 persistence assertion)"
      to: "apps/web/e2e/fixtures/api.ts (getIssue)"
      via: "api.getIssue(tgt.id) call"
      pattern: "api\\.getIssue"
    - from: "apps/web/e2e/specs/timeline-dependency-propagation.spec.ts (TEST-24 toast)"
      to: "packages/i18n/src/locales/en/translations.ts:2769,2772"
      via: "getByText with literal English strings"
      pattern: "Schedule update failed|missing start or target dates"
    - from: "apps/web/e2e/specs/timeline-dependency-propagation.spec.ts (TEST-24 setup)"
      to: "apps/web/e2e/fixtures/api.ts (clearIssueDate)"
      via: 'api.clearIssueDate(tgt.id, "target_date") AFTER propagationTimeline render'
      pattern: "api\\.clearIssueDate"
---

<objective>
Phase 6 Wave 2: Plan 06-01 で敷設したフィクスチャ + POM + API ヘルパーを土台に、PRD 要件 **TEST-23**(ハッピーパス)と **TEST-24**(失敗パス)を Playwright スペックとして実装する。マイルストーン v1.0 はこのプラン完了で全要件 GREEN になる。

Purpose: TEST-23 はサーバ権威の依存伝播がブラウザ→API→DB の全層でエンドツーエンドに機能することを証明する。TEST-24 は失敗時の reason code 経路(server 422 → wire envelope → MobX 失敗ブランチ → トースト → DOM ロールバック)が完全に可逆であり、ユーザに明確な理由が伝わることを証明する。両テストは Phase 1–5 の合計 12 プランの実装が **意図通りに統合された最終確認**である。

Output:

- TEST-23 + TEST-24 の 2 active tests を `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` に実装
- Wave 1 で配置した 4 smoke tests を `test.skip(...)` に変換(または削除) — TEST-23 / TEST-24 が同じ surface を完全カバーするため
- `apps/web/e2e/README.md` に locale 前提条件 1 行追加(D-08a)
- フルスイート 5 GREEN(既存 3 + TEST-23 + TEST-24) — 2 回連続で GREEN(冪等性確認)

Requirements covered:

- **TEST-23** (E2E happy path): drag → 依存 work item が動く → 永続化
- **TEST-24** (E2E failure path): drag → reject → UI が原状復帰
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-end-to-end-coverage-polish/06-CONTEXT.md
@.planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md
@.planning/phases/06-end-to-end-coverage-polish/06-PATTERNS.md
@.planning/phases/06-end-to-end-coverage-polish/06-VALIDATION.md
@.planning/phases/06-end-to-end-coverage-polish/06-01-PLAN.md
@apps/web/e2e/specs/timeline-dependency-propagation.spec.ts
@apps/web/e2e/specs/timeline-dependency-drag.spec.ts
@apps/web/e2e/pages/timeline.page.ts
@apps/web/e2e/fixtures/api.ts
@apps/web/e2e/fixtures/test-fixtures.ts
@apps/web/e2e/README.md
@packages/i18n/src/locales/en/translations.ts
@CLAUDE.md

<interfaces>
<!-- Wire contract (Phase 3 — locked): -->

POST `/api/workspaces/<slug>/projects/<projectId>/timeline-propagation/`
Request body (TimelinePropagationRequestSerializer):

```typescript
{
  work_item_id: string;            // UUID — drag された Issue ID
  original_start_date: string;     // YYYY-MM-DD — drag 開始時のスナップショット
  original_target_date: string;    // YYYY-MM-DD
  expected_updated_at: string;     // ISO 8601 with microseconds (Django default)
  requested_start_date: string;    // YYYY-MM-DD — drop 先の新 start_date
  requested_target_date: string;   // YYYY-MM-DD
  operation: "move";
  client_preview_count?: number;   // optional
}
```

Success response (HTTP 200):

```typescript
{
  requested_work_item_id: string;
  total_updated_count: number;
  client_preview_count: number | null;
  work_items: Array<{
    id: string;
    start_date: string;
    target_date: string;
    updated_at: string;
  }>;
}
```

Failure response (HTTP 422 for INCOMPLETE_SCHEDULE):

```typescript
{
  code: "INCOMPLETE_SCHEDULE";
  message: string;
}
```

STATUS_BY_CODE (Phase 3 D-03):

- `PERMISSION_DENIED` → 403
- `SCHEDULE_CHANGED` → 409
- `DEPENDENCY_CYCLE`/`PROJECT_BOUNDARY_EXCEEDED`/`INCOMPLETE_SCHEDULE`/`PROPAGATION_LIMIT_EXCEEDED`/`INVALID_DATE_RANGE` → 422

i18n strings (en):

- `timeline.propagation.error.title` → `"Schedule update failed"` (translations.ts:2769)
- `timeline.propagation.error.incomplete_schedule` → `"A dependent work item is missing start or target dates."` (translations.ts:2772)

Day spacing (D-06b — fixed by `propagationPair` fixture from Plan 06-01):

- src: today+0 ～ today+3 (3-day duration)
- tgt: today+5 ～ today+8 (3-day duration; 2-day gap to src)
- TEST-23 / TEST-24 drag delta: +4 days rightward (src.target +3 → +7 violates tgt.start = +5; propagation forces tgt to +8/+11)
  </interfaces>
  </context>

<tasks>

<task id="06-02-01" wave="2" depends_on="[06-01-07]" autonomous="true">
  <description>Implement TEST-23 (happy path): drag predecessor + 4 days → assert network 200 + DOM tgt-shift + DB persistence. Convert Wave 1 smoke tests to `test.skip(...)` so the spec file ends with 2 active + 4 skipped + 1 skipped placeholder.</description>
  <read_first>
    - apps/web/e2e/specs/timeline-dependency-propagation.spec.ts (current state — placeholder + 4 smoke tests from Plan 06-01)
    - apps/web/e2e/specs/timeline-dependency-drag.spec.ts (analog — `waitForResponse` before action pattern)
    - apps/web/e2e/fixtures/test-fixtures.ts (propagationPair / propagationTimeline)
    - apps/web/e2e/fixtures/api.ts (getIssue helper)
    - apps/web/e2e/pages/timeline.page.ts (dragBlockBy / getBlockBox / getDayWidthFromBlock)
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Wire Contract Confirmation" §1–§3
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"DOM Derivation Recipes" §2 (pre/post-drag boxes pattern)
    - .planning/phases/06-end-to-end-coverage-polish/06-PATTERNS.md §"Phase 6 specific: TEST-23 ..."
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Pitfall 2: MobX reactivity flush"
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Pitfall 7: waitForResponse を drag の後にセットする"
  </read_first>
  <action>
**Step 1 — Convert Wave 1 smoke tests to `test.skip`:**

Plan 06-01 が配置した 4 smoke tests(`#smoke: relation seed survives deletion cascade`, `#smoke: clearIssueDate sets target_date to null`, `#smoke: getIssue reads back ...`, `#smoke: dragBlockBy moves the dragged block visually`, `#smoke: propagationPair seeds relation ...`)を全て `test.skip(...)` に変える。理由: TEST-23 が同じ surface(relation 作成 + ドラッグ + 永続化読み出し + propagationPair)を完全カバーするため、smoke は冗長になる。

各 `test(...)` を `test.skip(...)` に置換:

```typescript
// Before:
test("#smoke: relation seed survives deletion cascade", async ({ api }, testInfo) => { ... });

// After:
test.skip("#smoke: relation seed survives deletion cascade", async ({ api }, testInfo) => { ... });
```

注意: 関数本体は残す(後で再実行できるように)。`test.skip` は実行時に確実に skip される。

代替: 完全削除も可。Plan 06-02 SUMMARY に判断理由を記載。**推奨は `test.skip(...)` 化**(変更が最小、後で診断で復活させられる)。

**Step 2 — Spec ファイル先頭の import を整える:**

Wave 1 で `import { test } from "../fixtures/test-fixtures"` のみだった場合、TEST-23 / TEST-24 は `expect` も使うため:

```typescript
import { test, expect } from "../fixtures/test-fixtures";
```

dynamic import (`await import("../fixtures/test-fixtures")`) を smoke 内で使っていた箇所も top-level import に統合(skip された関数本体は実行されないので、dynamic import が残っていても害はないが、可読性向上のため統合する)。

**Step 3 — TEST-23 を実装:**

placeholder の `test.skip("#1 [TEST-23] happy path: ...")` を `test("#1 [TEST-23] happy path: ...")` に変えて以下の本体を入れる:

```typescript
test("#1 [TEST-23] happy path: drag predecessor moves successor and persists", async ({
  page,
  api,
  propagationPair,
  propagationTimeline,
}) => {
  const { src, tgt } = propagationPair;

  // dayWidth は propagationPair の tgt(start+5/end+8、duration=4days)から DOM 派生
  const dayWidth = await propagationTimeline.getDayWidthFromBlock(tgt.id, tgt);
  if (dayWidth <= 0) {
    throw new Error(`unexpected dayWidth=${dayWidth} (DOM derivation failed)`);
  }

  // pre-drag バウンディングボックス(D-04 step 2 の比較対象)
  const preDragBoxSrc = await propagationTimeline.getBlockBox(src.id);
  const preDragBoxTgt = await propagationTimeline.getBlockBox(tgt.id);

  // waitForResponse は drag の前にセット(Pitfall 7)
  const responsePromise = page.waitForResponse(
    (r) => r.url().includes("/timeline-propagation/") && r.request().method() === "POST",
    { timeout: 15_000 }
  );

  // src を +4 日右にドラッグ — src.target=+3 → +7 が tgt.start=+5 を violate
  // → サーバが tgt を +8/+11 に shift
  await propagationTimeline.dragBlockBy(src.id, 4, src);

  const resp = await responsePromise;

  // === Assertion 1: Network ===
  expect(resp.status()).toBe(200);
  const body = (await resp.json()) as {
    requested_work_item_id: string;
    total_updated_count: number;
    work_items: Array<{ id: string; start_date: string; target_date: string; updated_at: string }>;
  };
  expect(body.requested_work_item_id).toBe(src.id);
  expect(body.total_updated_count).toBeGreaterThanOrEqual(2);

  // リクエストボディも確認(operation=move、work_item_id=src.id)
  expect(resp.request().postDataJSON()).toMatchObject({
    work_item_id: src.id,
    operation: "move",
  });

  // === Assertion 2: DOM ===
  // MobX runInAction の flush 後に tgt が右にシフトしていることを expect.poll で待つ(Pitfall 2)
  // dayWidth - 2px tolerance(D-04c)
  await expect
    .poll(async () => (await propagationTimeline.getBlockBox(tgt.id)).x, { timeout: 5_000 })
    .toBeGreaterThan(preDragBoxTgt.x + dayWidth - 2);

  // src(drag された側)も右にシフトしている(基準: pre-drag より dayWidth*4 - tolerance)
  await expect
    .poll(async () => (await propagationTimeline.getBlockBox(src.id)).x, { timeout: 5_000 })
    .toBeGreaterThan(preDragBoxSrc.x + dayWidth - 2);

  // === Assertion 3: Persistence ===
  // server response から期待値を取り出す
  const serverTgt = body.work_items.find((wi) => wi.id === tgt.id);
  expect(serverTgt, `server response did not include tgt(${tgt.id}) in work_items`).toBeDefined();

  // API GET で DB を直読み — bulk_update がコミット済みなら新値が返る(stale cache なし)
  const persisted = await api.getIssue(tgt.id);
  expect(persisted.start_date).toBe(serverTgt!.start_date);
  expect(persisted.target_date).toBe(serverTgt!.target_date);
});
```

注意:

- `propagationTimeline` の構築時点で `gotoIssueGantt()` + `waitForBlock(src.id)` + `waitForBlock(tgt.id)` が完了している(Plan 06-01 06-01-06 の fixture 定義)
- `waitForResponse` は drag の前に Promise を作成する(Pitfall 7) — Wait 5 秒以内にレスポンスが届く想定
- `expect.poll` は MobX `runInAction` のバッチ書き込みが React re-render を引き起こすまでポーリング(Pitfall 2)
- DOM 比較に `dayWidth - 2px` を使う(±2px 公差、D-04c)
- `body.work_items.find(...)` の `!` non-null assertion は直前で `expect(serverTgt).toBeDefined()` してるので OK

**Step 4 — `pnpm --filter=web test:e2e --grep "TEST-23"` を実行:**

```bash
pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-23"
```

期待: 1 passed (placeholder skip が active test に変わって 1 passed)。

万一 fail したら:

- `body.total_updated_count` が 1 ならサーバが tgt を伝播していない — propagationPair の day spacing(D-06b)とサーバの伝播ロジックを再確認
- `expect.poll` がタイムアウトしたら timeout を 8_000ms に伸ばす(D-12b は 5_000 を提案するが MobX reactivity が遅い場合あり)
- `getDayWidthFromBlock` の dayWidth が 0 なら off-by-one(Pitfall 3) — `Math.round((end - start) / msPerDay) + 1` 公式を確認

**Step 5 — Commit:**

```
test(06-02): implement TEST-23 happy path E2E spec

3-tier assertion (network 200 + DOM shift + DB persistence) GREEN.
Wave 1 smoke tests converted to test.skip().
```

  </action>
  <acceptance_criteria>
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c '^  test."#1 \[TEST-23\] happy path' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1` (active test, not skip)
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c 'test.skip."#smoke' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns at least `4` (smokes converted to skip; placeholder #2 still skipped is also counted, allow ≥4)
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c 'expect(resp.status()).toBe(200)' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns at least `1`
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c 'expect(body.total_updated_count).toBeGreaterThanOrEqual(2)' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c 'api.getIssue(tgt.id)' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c 'import.*test.*expect.*from.*test-fixtures' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c 'expect.poll' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns at least `2`
    - Verification command: `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-23"` exits 0 with `1 passed`
    - Existing 3 specs still GREEN: `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "timeline dependency drag"` exits 0 with `3 passed`
  </acceptance_criteria>
  <verification>
    <command>pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-23" 2>&amp;1 | tail -15</command>
    <expect>1 passed; exit code 0</expect>
  </verification>
</task>

<task id="06-02-02" wave="2" depends_on="[06-02-01]" autonomous="true">
  <description>Implement TEST-24 (failure path): clearIssueDate(tgt, "target_date") AFTER timeline render → drag → assert 422 + INCOMPLETE_SCHEDULE toast + DOM rollback both blocks within ±2px.</description>
  <read_first>
    - apps/web/e2e/specs/timeline-dependency-propagation.spec.ts (current state with TEST-23 GREEN)
    - apps/web/e2e/fixtures/api.ts (clearIssueDate semantics)
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Wire Contract Confirmation" §4
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Toast Selector Strategy"
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Failure-Path Behavior" — full Phase 1–7 trace
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Pitfall 5: isBlockComplete ガード"
    - .planning/phases/06-end-to-end-coverage-polish/06-PATTERNS.md §"Phase 6 specific: TEST-24 ..."
    - packages/i18n/src/locales/en/translations.ts:2766-2783 (toast strings)
  </read_first>
  <action>
TEST-24 の placeholder (`test.skip("#2 [TEST-24] failure path: ...")`) を active test に変えて以下の本体を入れる:

```typescript
test("#2 [TEST-24] failure path: incomplete-schedule rejects drag and rolls back UI", async ({
  page,
  api,
  propagationPair,
  propagationTimeline,
}) => {
  const { src, tgt } = propagationPair;

  // === Setup (D-07a: clearIssueDate AFTER propagationTimeline 完了) ===
  // propagationTimeline fixture は既に gotoIssueGantt + waitForBlock(src,tgt) 済み。
  // ブラウザのローカル MobX ストアは tgt.target_date を populated でホールド(D-07b — WS 通知なし)。
  // 今 server-side のみクリアすることで、UI の isBlockComplete ガードは true のまま — drag が発火する。
  await api.clearIssueDate(tgt.id, "target_date");

  // dayWidth は src(start+0/end+3、duration=4days)から DOM 派生
  // 注: tgt は target_date が server-side では null だが local 表示は populated のため
  //     getDayWidthFromBlock(tgt, tgt) は依然 4-day duration として動く
  const dayWidth = await propagationTimeline.getDayWidthFromBlock(src.id, src);

  // pre-drag バウンディングボックス(rollback 比較用、D-04a step 3)
  const preDragBoxSrc = await propagationTimeline.getBlockBox(src.id);
  const preDragBoxTgt = await propagationTimeline.getBlockBox(tgt.id);

  // waitForResponse を drag の前にセット(Pitfall 7)
  const responsePromise = page.waitForResponse(
    (r) => r.url().includes("/timeline-propagation/") && r.request().method() === "POST",
    { timeout: 15_000 }
  );

  // src を +4 日右にドラッグ → server は tgt の target_date=null を検知 → 422 + INCOMPLETE_SCHEDULE
  await propagationTimeline.dragBlockBy(src.id, 4, src);

  const resp = await responsePromise;

  // === Assertion 1: Network — 422 + envelope ===
  expect(resp.status()).toBe(422);
  const body = (await resp.json()) as { code: string; message: string };
  expect(body).toMatchObject({ code: "INCOMPLETE_SCHEDULE" });
  expect(typeof body.message).toBe("string");
  expect(body.message.length).toBeGreaterThan(0);

  // === Assertion 2: Toast — text-based seam(@plane/propel/toast に data-testid なし)===
  // 英語 i18n 文字列は `packages/i18n/src/locales/en/translations.ts:2769,2772` 参照
  await expect(page.getByText("Schedule update failed")).toBeVisible({ timeout: 8_000 });
  await expect(page.getByText("A dependent work item is missing start or target dates.")).toBeVisible();

  // === Assertion 3: DOM rollback — both blocks return to pre-drag positions(D-04c, ±2px)===
  // previewById.clear() が runInAction 内で実行される → block.position fallback で元位置に
  // expect.poll で MobX flush を待つ(Pitfall 2)
  await expect
    .poll(async () => (await propagationTimeline.getBlockBox(src.id)).x, { timeout: 5_000 })
    .toBeGreaterThanOrEqual(preDragBoxSrc.x - 2);
  await expect
    .poll(async () => (await propagationTimeline.getBlockBox(src.id)).x, { timeout: 5_000 })
    .toBeLessThanOrEqual(preDragBoxSrc.x + 2);
  await expect
    .poll(async () => (await propagationTimeline.getBlockBox(tgt.id)).x, { timeout: 5_000 })
    .toBeGreaterThanOrEqual(preDragBoxTgt.x - 2);
  await expect
    .poll(async () => (await propagationTimeline.getBlockBox(tgt.id)).x, { timeout: 5_000 })
    .toBeLessThanOrEqual(preDragBoxTgt.x + 2);
});
```

注意:

- `clearIssueDate` の呼び出しは **`propagationTimeline` 構築の後** — この順序が D-07a / D-12a の核心。`propagationTimeline` fixture が `gotoIssueGantt + waitForBlock` を実行することで、ブラウザは populated tgt をローカルストアに持つ。その直後に `clearIssueDate` が server-side のみ変更しても、ブラウザは知らない → `isBlockComplete = true` → drag が発火 → server が 422 を返す
- `expect.poll(...).toBeGreaterThanOrEqual(...).toBeLessThanOrEqual(...)` の 2 段ポーリングで ±2px tolerance を表現(`toBeCloseTo` は単一 expectation で表現できないため分離)
- 別解: helper を作って単一ポーリングで `Math.abs(box.x - pre.x) <= 2` を assert することも可。可読性で選択
- `dayWidth` 変数は使わなくなった(rollback 比較は preDragBox の位置のみ使用) — 削除しても良い。残しておけば smoke 用に再利用しやすい
- toast `getByText` は `timeout: 8_000ms`(default 5_000ms toast auto-dismiss と被らないよう、最初の assertion に余裕を持たせる)

**追加: README.md 更新(D-08a)**

`apps/web/e2e/README.md` の「前提」または冒頭セクションに 1 行追記:

```markdown
- **UI 言語 = `en`**: `timeline-dependency-propagation.spec.ts`(TEST-24)はトーストの英語文字列をアサートするため、テスト用ワークスペースの UI 言語を English に設定してください(`ja` で実行すると失敗します — D-04b / D-08a)。
```

注意: `apps/web/e2e/README.md` の現状を読み、適切な見出し配下に挿入する。既存の前提条件箇条書きがあればその直下に追加。なければ「## 前提」または「## Prerequisites」セクションを作って追加。

**実行:**

```bash
pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-24"
```

期待: 1 passed.

万一 fail したら:

- `expect(resp.status()).toBe(422)` が 200 で fail → `clearIssueDate` が前のテストの teardown より前に呼ばれていないか確認 → fixture 順序を再確認
- toast `getByText` がタイムアウト → ロケールが `ja` の可能性 → README 前提条件を確認、ワークスペースを `en` に切り替え
- DOM rollback の `expect.poll` がタイムアウト → MobX `previewById.clear()` が呼ばれていない → Phase 4/5 store の `_doCommit` 失敗ブランチを確認(リファレンス実装は Plan 04-02 / 05-02 の SUMMARY.md)

**Commit:**

```
test(06-02): implement TEST-24 failure path E2E spec + locale precondition note

3-tier assertion (422 envelope + toast text + DOM rollback within ±2px) GREEN.
README updated with en locale precondition (D-08a).
```

  </action>
  <acceptance_criteria>
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c '^  test."#2 \[TEST-24\] failure path' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c 'api.clearIssueDate(tgt.id, "target_date")' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c 'expect(resp.status()).toBe(422)' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c 'INCOMPLETE_SCHEDULE' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns at least `1`
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c "Schedule update failed" apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c "A dependent work item is missing start or target dates" apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` `grep -c 'preDragBoxTgt' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns at least `2` (capture + assert)
    - `apps/web/e2e/README.md` contains a locale precondition mentioning `en`: `grep -c "en" apps/web/e2e/README.md` increases by at least 1 vs HEAD~1
    - Verification command 1: `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-24"` exits 0 with `1 passed`
    - Verification command 2: `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "timeline dependency"` exits 0 with at least `5 passed` (existing 3 + TEST-23 + TEST-24)
  </acceptance_criteria>
  <verification>
    <command>pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-24" 2>&amp;1 | tail -15</command>
    <expect>1 passed; exit code 0</expect>
  </verification>
</task>

<task id="06-02-03" wave="2" depends_on="[06-02-02]" autonomous="true">
  <description>Full-suite idempotency check: run `pnpm --filter=web test:e2e` twice consecutively. Both runs must pass with identical test count. Lint + types final gate.</description>
  <read_first>
    - apps/web/e2e/specs/timeline-dependency-propagation.spec.ts (final state — 2 active + skips)
    - apps/web/e2e/specs/timeline-dependency-drag.spec.ts (regression guard)
    - .planning/phases/06-end-to-end-coverage-polish/06-CONTEXT.md §"D-07" — cleanup contract
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Pitfall 6: propagationPair の teardown" — cascade
    - .planning/phases/06-end-to-end-coverage-polish/06-VALIDATION.md §"Sampling Rate" — idempotency check
  </read_first>
  <action>
**Step 1 — フルスイート 1 回目:**

```bash
pnpm --filter=web test:e2e 2>&1 | tee /tmp/e2e-run-1.log | tail -50
```

期待:

- 既存 3 specs (`timeline dependency drag` describe) → 3 passed
- TEST-23 + TEST-24 → 2 passed
- 4–5 smoke tests skipped (Plan 06-02 06-02-01 で `test.skip` 化)
- Total: ≥ 5 passed; 0 failed

**Step 2 — フルスイート 2 回目(冪等性確認):**

```bash
pnpm --filter=web test:e2e 2>&1 | tee /tmp/e2e-run-2.log | tail -50
```

期待: 1 回目と完全同一の結果。

冪等性が崩れる原因(あれば):

- `propagationPair` の teardown が失敗 → `IssueRelation` 行が残留(ON DELETE CASCADE が機能しているはずなので普通は起きない)
- `clearIssueDate` で null 化された tgt が削除されず残留(`deleteIssue` は null dates を許容するため通常 OK)
- 1 回目のテストで作られた issue ID が 2 回目のテストで再生成されようとする(suffix に `Date.now()` を含めているので衝突しないはず)
- 同名 issue がワークスペースに残留して unique constraint 違反 → 既存 specs と同じく許容(Plane の Issue は名前重複可)

**Step 3 — Lint + Types ゲート:**

```bash
pnpm --filter=web check:lint 2>&1 | tail -10
pnpm --filter=web check:types 2>&1 | tail -10
```

期待:

- check:lint: 警告数 ≤ 11957(OxLint budget)
- check:types: 0 errors

**Step 4 — Verify 3 existing specs unchanged:**

`timeline-dependency-drag.spec.ts` を read してファイル内容が Phase 1 終了時点と byte-identical であることを確認:

```bash
git diff origin/feature/timeline-dependency-drag~30 -- apps/web/e2e/specs/timeline-dependency-drag.spec.ts
```

(具体的な commit hash は不明 — Phase 1 完了時の commit を SUMMARY 履歴から特定)

**Step 5 — Phase 6 SUMMARY 用にメトリクス記録:**

以下を Plan 06-02 SUMMARY に記載:

- フルスイート 1 回目の所要時間 (Playwright reporter 末尾出力 `X passed (Ym Zs)`)
- フルスイート 2 回目の所要時間
- アクティブテスト総数 / passed / skipped / failed の内訳
- OxLint 警告数(Plan 06-01 末尾値からの差分)

**Step 6 — REQUIREMENTS.md 更新の準備(commit に含めない、SUMMARY に記載):**

`/gsd-verify-work` の責務だが、Plan 06-02 が **TEST-23 と TEST-24 の両方を GREEN にした** ことを SUMMARY で明示する:

```
TEST-23 ✅ GREEN — apps/web/e2e/specs/timeline-dependency-propagation.spec.ts:#1
TEST-24 ✅ GREEN — apps/web/e2e/specs/timeline-dependency-propagation.spec.ts:#2
```

**Commit:**

```
test(06-02): full-suite idempotency check + final lint/types gate

Run 1: <count> passed in <duration>
Run 2: <count> passed in <duration>
OxLint: <warnings>/11957
TypeScript: clean
TEST-23 + TEST-24 GREEN — milestone v1.0 complete pending /gsd-verify-work
```

注意:

- このタスクはコード変更なし(検証のみ)— ただし所要時間とメトリクスを SUMMARY に書く必要があるため commit は実施(空 commit で SUMMARY 用 commit message を残す)
- もし実際にコードの不具合が見つかれば、Plan 06-02 06-02-01 / 06-02-02 を再修正してから commit
  </action>
  <acceptance_criteria> - Verification command 1: `pnpm --filter=web test:e2e` (run 1) exits 0 with ≥ 5 passed, 0 failed - Verification command 2: `pnpm --filter=web test:e2e` (run 2) exits 0 with same passed count as run 1, 0 failed - Verification command 3: `pnpm --filter=web check:lint` warning count ≤ 11957 - Verification command 4: `pnpm --filter=web check:types` exits 0 - `apps/web/e2e/specs/timeline-dependency-drag.spec.ts` byte-identical from Phase 1 end: `git diff $(git log --format=%H -- apps/web/e2e/specs/timeline-dependency-drag.spec.ts | tail -1) HEAD -- apps/web/e2e/specs/timeline-dependency-drag.spec.ts` is empty - No production code modified throughout Plan 06: `git diff origin/preview...HEAD -- apps/web/core apps/web/ce apps/api packages` shows no changes ATTRIBUTABLE to Plan 06 commits (Plan 6 commits are scoped to apps/web/e2e + .planning/\* + apps/web/e2e/README.md only) - Plan 06-02 SUMMARY.md drafted with run-time metrics and TEST-23/TEST-24 GREEN markers
  </acceptance_criteria>
  <verification>
  <command>pnpm --filter=web test:e2e 2>&amp;1 | tail -10 &amp;&amp; echo "--- run 2 ---" &amp;&amp; pnpm --filter=web test:e2e 2>&amp;1 | tail -10 &amp;&amp; pnpm --filter=web check:lint 2>&amp;1 | tail -3 &amp;&amp; pnpm --filter=web check:types 2>&amp;1 | tail -3</command>
  <expect>Run 1: ≥5 passed, 0 failed. Run 2: same. Lint clean (≤11957). Types clean.</expect>
  </verification>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                     | Description                                                                                                                                                                                   |
| ------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Playwright APIRequestContext → Django API (`clearIssueDate`) | TEST-24 setup mutates Issue.target_date to null on the dev DB; CSRF + session-cookie reuse from Plan 06-01 helper                                                                             |
| Playwright browser → Django API (drag → propagation)         | TEST-23 / TEST-24 trigger the production `/timeline-propagation/` endpoint via `dragBlockBy`; success persists tgt's new dates, failure is rolled back server-side via `transaction.atomic()` |
| Toast text rendering → Test assertion                        | TEST-24's `getByText` literally matches i18n English strings; locale drift = false-fail                                                                                                       |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                            | Disposition | Mitigation Plan                                                                                                                                                     |
| ---------- | -------------------------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-02-01 | T (Tampering)              | TEST-24 `clearIssueDate` setup       | mitigate    | CSRF + session-cookie auth via Plan 06-01 helper; targets `propagationPair.tgt.id` only (test-scoped)                                                               |
| T-06-02-02 | I (Information disclosure) | TEST-24 toast text assertion         | accept      | i18n strings are public UI; no secrets surfaced; the test FAILS if strings change without translation update — this is the contract                                 |
| T-06-02-03 | D (Denial of service)      | TEST suite idempotency               | mitigate    | Task 06-02-03 enforces 2 consecutive full-suite runs; cleanup via `Promise.allSettled` + cascade-on-delete from Plan 06-01                                          |
| T-06-02-04 | T (Tampering)              | TEST-23 `getIssue` persistence read  | mitigate    | DRF `IssueViewSet.retrieve` reads from authoritative DB row; no Redis cache layer (verified RESEARCH §Persistence Read Path)                                        |
| T-06-02-05 | E (Elevation of privilege) | Workspace data leakage between tests | mitigate    | Each test creates its own `propagationPair` with `Date.now()` suffix; teardown deletes both Issues; `IssueRelation.issue` cascade ensures relation rows are removed |

</threat_model>

<verification>
End-of-Plan-06-02 / End-of-Phase-6 / End-of-Milestone checks:

- [ ] `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` contains 2 active tests (TEST-23 + TEST-24) and the Wave 1 smokes are skipped or removed
- [ ] `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-23"` → 1 passed, 0 failed
- [ ] `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-24"` → 1 passed, 0 failed
- [ ] `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "timeline dependency drag"` → 3 passed (regression guard)
- [ ] `pnpm --filter=web test:e2e` → ≥5 passed, 0 failed (run 1)
- [ ] `pnpm --filter=web test:e2e` → identical result (run 2 — idempotency)
- [ ] `pnpm --filter=web check:lint` ≤ 11957 warnings
- [ ] `pnpm --filter=web check:types` clean
- [ ] `apps/web/e2e/README.md` updated with `en` locale precondition (D-08a)
- [ ] No production code modified across all Plan 06 commits (apps/web/core, apps/web/ce, apps/api, packages all byte-identical)
- [ ] No `playwright.config.ts` / `auth.setup.ts` / `package.json` / `turbo.json` / `.gitignore` modifications
- [ ] REQUIREMENTS.md TEST-23 + TEST-24 ready to flip to ✅ at `/gsd-verify-work` (Plan 06-02 does NOT modify REQUIREMENTS.md — that is the verify command's responsibility)
      </verification>

<success_criteria>
Plan 06-02 ships when:

- All 3 tasks committed atomically (one commit per task; Task 06-02-03 may be a verification-only commit if no code changes were needed)
- Phase 6 closed: TEST-23 + TEST-24 are the last 2 ✅ marks needed for milestone v1.0 (REQUIREMENTS.md will reach 100% upon `/gsd-verify-work`)
- Plan 06-02 SUMMARY records:
  - Total active tests in suite (5 = 3 existing + 2 new)
  - Skipped count (4–5 smokes from Plan 06-01)
  - Run-1 and Run-2 wall-clock times
  - OxLint warning count diff (Plan 06-01 baseline → Plan 06-02 final)
  - TEST-23 and TEST-24 GREEN markers with file:line references
- Project STATE.md ready to flip `status: complete` (set during `/gsd-verify-work`)
  </success_criteria>

<output>
After completion, create `.planning/phases/06-end-to-end-coverage-polish/06-02-SUMMARY.md`
</output>
