---
plan_id: 06-01
phase: 6
wave: 1
depends_on: []
requirements: []
files_modified:
  - apps/web/e2e/fixtures/api.ts
  - apps/web/e2e/fixtures/test-fixtures.ts
  - apps/web/e2e/pages/timeline.page.ts
files_created:
  - apps/web/e2e/specs/timeline-dependency-propagation.spec.ts
autonomous: true
estimated_duration: 25m
must_haves:
  truths:
    - 'placeholder spec exists and `pnpm --filter=web test:e2e --grep "placeholder"` reports 1 skipped, 0 failed (D-13a self-test)'
    - '`Api.createIssueRelation(srcId, tgtId, "blocking")` POSTs to `/api/workspaces/<slug>/projects/<id>/issues/<srcId>/issue-relation/` and asserts 201'
    - '`Api.clearIssueDate(issueId, "target_date")` PATCHes `{ target_date: null }` to `/api/workspaces/<slug>/projects/<id>/issues/<id>/` and accepts 200 or 204'
    - "`Api.getIssue(issueId)` GETs the issue and returns a `CreatedIssue` with non-null `start_date` / `target_date`"
    - "`TimelinePage.dragBlockBy(issueId, deltaDays, issue)` moves the dragged block's bounding box by ~`deltaDays * dayWidth` after `mouse.up`"
    - "`TimelinePage.getBlockBox(issueId)` returns a `{x,y,width,height}` object derived from `[data-block-id=...]` boundingBox"
    - "`propagationPair` fixture creates src (start+0/end+3) + tgt (start+5/end+8) + a `blocking` relation src→tgt and cascade-cleans on teardown"
    - "`propagationTimeline` fixture renders both blocks via `waitForBlock` after `gotoIssueGantt`"
    - "Existing 3 relation-creation specs (`#1`/`#2`/`#3` in `timeline-dependency-drag.spec.ts`) still pass — additive changes only"
    - "`pnpm --filter=web check:lint` produces 0 new warnings (budget 11957 unchanged)"
    - "`pnpm --filter=web check:types` exits 0"
  artifacts:
    - path: "apps/web/e2e/specs/timeline-dependency-propagation.spec.ts"
      provides: "Placeholder spec file with skipped TEST-23 / TEST-24 stubs (Plan 06-02 fills in the bodies)"
      contains: 'test.describe("timeline dependency propagation"'
    - path: "apps/web/e2e/fixtures/api.ts"
      provides: "Api class with createIssueRelation / clearIssueDate / getIssue methods"
      contains: "createIssueRelation"
    - path: "apps/web/e2e/fixtures/test-fixtures.ts"
      provides: "propagationPair + propagationTimeline fixtures wired into base.extend<Fixtures>"
      contains: "propagationPair"
    - path: "apps/web/e2e/pages/timeline.page.ts"
      provides: "TimelinePage with dragBlockBy / getBlockBox / getDayWidthFromBlock methods"
      contains: "dragBlockBy"
  key_links:
    - from: "apps/web/e2e/fixtures/test-fixtures.ts (propagationPair)"
      to: "apps/web/e2e/fixtures/api.ts (createIssueRelation)"
      via: "fixture seeding hook"
      pattern: "api\\.createIssueRelation"
    - from: "apps/web/e2e/pages/timeline.page.ts (dragBlockBy)"
      to: "apps/web/core/components/gantt-chart/helpers/draggable.tsx onMouseDown wire"
      via: "page.mouse.move + page.mouse.down + page.mouse.move(steps:20) + page.mouse.up"
      pattern: "page\\.mouse\\.(down|up|move)"
    - from: "apps/web/e2e/specs/timeline-dependency-propagation.spec.ts (placeholder)"
      to: "apps/web/e2e/fixtures/test-fixtures.ts (test export)"
      via: "import { test } from '../fixtures/test-fixtures'"
      pattern: "import \\{ test"
---

<objective>
Phase 6 Wave 1: 既存の Playwright インフラ (`apps/web/e2e/`) に対して **加法的にのみ** 変更を加え、Wave 2 で TEST-23 / TEST-24 を実装するための完全なフィクスチャ + POM + API ヘルパーを敷設する。

Purpose: Plan 06-02 の TEST-23 / TEST-24 が完全な仕様アサーションのみに集中できるよう、ドラッグメカニクス・リレーションシード・日付クリア・永続化読み出しなどの **間接的な配管** をすべて先行して smoke-test 済みの状態で揃える。各タスクは個別の smoke `test()` で自己検証し、インフラ問題（Chromium バイナリ未インストール、`.env.e2e` 設定漏れ、`storageState` 失効、PATCH ステータスコードの想定外、Plane Issue serializer の null 受付可否）を Wave 2 のスペック実装と分離して早期に発見する。

Output:

- Placeholder spec ファイル 1 件（NEW）
- `apps/web/e2e/fixtures/api.ts` に 3 メソッド追加（MODIFY — 既存メソッドは byte-identical）
- `apps/web/e2e/fixtures/test-fixtures.ts` に 2 フィクスチャ追加（MODIFY — 既存フィクスチャは byte-identical）
- `apps/web/e2e/pages/timeline.page.ts` に 3 メソッド追加（MODIFY — 既存メソッドは byte-identical）
- Plan 06-01 完了時点で `pnpm --filter=web test:e2e` が **5 GREEN**（既存 3 + Plan 06-01 が一時追加する smoke 2 — タスク末尾で skip / 削除する場合あり）または **3 GREEN + placeholder skipped** で安定していること。
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-end-to-end-coverage-polish/06-CONTEXT.md
@.planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md
@.planning/phases/06-end-to-end-coverage-polish/06-PATTERNS.md
@.planning/phases/06-end-to-end-coverage-polish/06-VALIDATION.md
@apps/web/e2e/specs/timeline-dependency-drag.spec.ts
@apps/web/e2e/pages/timeline.page.ts
@apps/web/e2e/fixtures/api.ts
@apps/web/e2e/fixtures/test-fixtures.ts
@apps/web/e2e/fixtures/env.ts
@apps/web/e2e/auth/auth.setup.ts
@apps/web/e2e/playwright.config.ts
@apps/web/e2e/README.md
@CLAUDE.md

<interfaces>
<!-- Existing public surface the executor MUST respect (byte-identical preservation required). -->

From apps/web/e2e/fixtures/api.ts:

```typescript
export type CreatedIssue = {
  id: string;
  name: string;
  start_date: string;
  target_date: string;
};
export async function createApi(): Promise<Api>;
export class Api {
  constructor(ctx: APIRequestContext, csrf: string);
  async createIssue(name: string, daysFromNow?: { start: number; end: number }): Promise<CreatedIssue>;
  async deleteIssue(issueId: string): Promise<void>;
  async dispose(): Promise<void>;
  // Plan 06-01 ADDS (additive only):
  async createIssueRelation(
    srcIssueId: string,
    targetIssueId: string,
    relationType?: "blocking" | "blocked_by" | "relates_to" | "duplicate"
  ): Promise<void>;
  async clearIssueDate(issueId: string, field: "start_date" | "target_date"): Promise<void>;
  async getIssue(issueId: string): Promise<CreatedIssue>;
}
```

From apps/web/e2e/pages/timeline.page.ts:

```typescript
const DRAG_STEPS = 20; // existing constant — reuse, do not redefine
export class TimelinePage {
  constructor(page: Page);
  async gotoIssueGantt(): Promise<void>;
  async waitForBlock(issueId: string): Promise<void>;
  block(issueId: string): Locator;
  async dragRightTo(sourceIssueId: string, targetIssueId: string, options?: { shiftKey?: boolean }): Promise<void>;
  async dragLeftTo(sourceIssueId: string, targetIssueId: string, options?: { shiftKey?: boolean }): Promise<void>;
  get picker(): Locator;
  async clickPickerOption(option: "blocking" | "blocked_by" | "relates_to" | "duplicate"): Promise<void>;
  // Plan 06-01 ADDS (additive only):
  async getBlockBox(issueId: string): Promise<{ x: number; y: number; width: number; height: number }>;
  async getDayWidthFromBlock(issueId: string, issue: { start_date: string; target_date: string }): Promise<number>;
  async dragBlockBy(
    issueId: string,
    deltaDays: number,
    issue: { start_date: string; target_date: string }
  ): Promise<void>;
}
```

From apps/web/e2e/fixtures/test-fixtures.ts:

```typescript
type Fixtures = {
  api: Api;
  issuePair: { src: CreatedIssue; tgt: CreatedIssue };
  timeline: TimelinePage;
  // Plan 06-01 ADDS:
  propagationPair: { src: CreatedIssue; tgt: CreatedIssue };
  propagationTimeline: TimelinePage;
};
export const test = base.extend<Fixtures>({...});
export { expect } from "@playwright/test";
```

Wire endpoints (READ-ONLY — server-side):

- `POST /api/workspaces/<slug>/projects/<id>/issues/<srcId>/issue-relation/` body `{ relation_type, issues: [tgtId] }` → 201
- `PATCH /api/workspaces/<slug>/projects/<id>/issues/<id>/` body `{ [field]: null }` → 200 or 204
- `GET /api/workspaces/<slug>/projects/<id>/issues/<id>/` → 200 with full issue body

Day-spacing contract (D-06b):

- src: `start: 0, end: 3` (3-day duration, today through today+3)
- tgt: `start: 5, end: 8` (3-day duration, today+5 through today+8)
- 2-day gap; rightward drag of `+4` days creates boundary violation
  </interfaces>
  </context>

<tasks>

<task id="06-01-01" wave="1" depends_on="[]" autonomous="true">
  <description>D-13a self-test: create placeholder spec file with skipped TEST-23 / TEST-24 stubs and verify the harness still runs.</description>
  <read_first>
    - apps/web/e2e/specs/timeline-dependency-drag.spec.ts (analog — exact import + describe pattern)
    - apps/web/e2e/fixtures/test-fixtures.ts (the `test` export to import from)
    - apps/web/e2e/playwright.config.ts (read-only — confirm config path)
    - .planning/phases/06-end-to-end-coverage-polish/06-CONTEXT.md §"D-13a"
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"First Minimum Task"
    - .planning/phases/06-end-to-end-coverage-polish/06-PATTERNS.md §"Imports pattern"
  </read_first>
  <action>
Create `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` with **exactly** the following content (verbatim — copy-paste, do not improvise):

```typescript
import { test } from "../fixtures/test-fixtures";

/**
 * Phase 6 — Timeline Dependency Schedule Propagation E2E coverage.
 *
 * 対象要件:
 *   TEST-23: drag predecessor → dependent work item moves → schedule persists end-to-end
 *   TEST-24: drag triggers known protocol error (INCOMPLETE_SCHEDULE) → UI rolls back + error toast
 *
 * 前提:
 *   - docker-compose-local.yml + pnpm dev が稼働
 *   - apps/web/e2e/.env.e2e 設定済み
 *   - ワークスペースの UI 言語 = en (D-04b / D-08a)
 *   - Issue layout = Gantt
 */
test.describe("timeline dependency propagation", () => {
  test.skip("#1 [TEST-23] happy path: drag predecessor moves successor and persists", async () => {
    // Plan 06-02 で実装する。Plan 06-01 では D-13a の self-test として skip 状態で配置する。
  });

  test.skip("#2 [TEST-24] failure path: incomplete-schedule rejects drag and rolls back UI", async () => {
    // Plan 06-02 で実装する。
  });
});
```

注意:

- `import { test, expect }` ではなく `import { test }` のみ — placeholder では `expect` を使わないため
- `test.skip(...)` は **第二引数を空関数** にする（async 関数を渡さないと Playwright が "missing test body" を報告する）
- describe ラベル `"timeline dependency propagation"` は CONTEXT D-01 に従って固定
- テスト名は CONTEXT D-01b / D-11 に従って `[TEST-NN]` タグ付き — `--grep "[TEST-23]"` でマッチする
- ファイル先頭の Japanese inline comment は既存スペックの voice (D-11a) を踏襲

実行後、self-test を行う:

```bash
pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-23"
```

期待される出力 (skipped と reported される — failures は 0):

```
1 skipped (...)
```

NB: `--grep "\[TEST-23\]"` は shell によっては `[]` がエスケープされて挙動が変わる。grep パターンは Playwright 内部で正規表現として評価されるが、エスケープなしの `--grep "TEST-23"` でも `[TEST-23]` を含むテスト名にマッチするので、エスケープ問題を避けるため後者を使う。

`pnpm --filter=web test:e2e` のスクリプト定義を `apps/web/package.json` で確認する（既存に対応）。`test:e2e` 経由で `--grep` を渡せない場合は `playwright test` 直叩きで代替する:

```bash
pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-23"
```

Auth setup は自動で実行される（`playwright.config.ts` の projects 設定に `setup` が含まれており、E2E `chromium` プロジェクトは `dependencies: ["setup"]`）。`auth.setup.ts` が UI ログイン → `apps/web/playwright/.auth/user.json` に storageState を書き出す。

Commit message:

```
test(06-01): add placeholder timeline-dependency-propagation spec for D-13a self-test
```

  </action>
  <acceptance_criteria>
    - File exists at exact path: `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts`
    - File content `grep -c 'test.describe."timeline dependency propagation"' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - File content `grep -c 'test.skip."#1 \[TEST-23\] happy path' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - File content `grep -c 'test.skip."#2 \[TEST-24\] failure path' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - File content `grep -c "import { test } from \"\.\./fixtures/test-fixtures\"" apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - Verification command: `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-23"` exits 0 and stdout contains `1 skipped` (or `2 skipped` if both are matched by substring; either is acceptable as long as failures are 0)
    - No production code (`apps/web/core/`, `apps/web/ce/`) modified — `git diff --stat -- apps/web/core apps/web/ce` produces empty output
    - No `.env.e2e.example` modified
  </acceptance_criteria>
  <verification>
    <command>pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-23" 2>&amp;1 | tail -20</command>
    <expect>0 failed; at least 1 skipped reported; exit code 0</expect>
  </verification>
</task>

<task id="06-01-02" wave="1" depends_on="[06-01-01]" autonomous="true">
  <description>Add `createIssueRelation` to `Api` class + smoke-test relation seeding.</description>
  <read_first>
    - apps/web/e2e/fixtures/api.ts (current state — verify lines 36-76)
    - apps/web/e2e/specs/timeline-dependency-drag.spec.ts (lines 14-19 — wire body shape `{ relation_type, issues: [tgtId] }` confirmed)
    - apps/web/e2e/fixtures/env.ts (env var contract)
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"API Helper Recipes §1"
    - .planning/phases/06-end-to-end-coverage-polish/06-PATTERNS.md §"`apps/web/e2e/fixtures/api.ts`" — `createIssueRelation` snippet
    - apps/api/plane/app/urls/issue.py:236-239 (URL `/issues/<srcId>/issue-relation/` confirmation)
    - apps/api/plane/db/models/issue.py:287-289 (cascade-on-delete confirmation)
  </read_first>
  <action>
Append `createIssueRelation` method to the `Api` class in `apps/web/e2e/fixtures/api.ts` **after** `deleteIssue` (so the existing methods stay byte-identical and the diff is purely additive). Insert exactly:

```typescript
  /**
   * 2 つの Issue 間にリレーションをシードする(D-05)。
   *
   * URL: POST /api/workspaces/<slug>/projects/<projectId>/issues/<srcIssueId>/issue-relation/
   * Body: { relation_type, issues: [targetIssueId] }
   * Expected: 201 Created
   *
   * カスケード削除: IssueRelation.issue は ON DELETE CASCADE なので、
   * deleteIssue(srcIssueId) で関係行も自動削除される(D-05b)。
   */
  async createIssueRelation(
    srcIssueId: string,
    targetIssueId: string,
    relationType: "blocking" | "blocked_by" | "relates_to" | "duplicate" = "blocking"
  ): Promise<void> {
    const resp = await this.ctx.post(
      `/api/workspaces/${env.workspaceSlug}/projects/${env.projectId}/issues/${srcIssueId}/issue-relation/`,
      {
        data: { relation_type: relationType, issues: [targetIssueId] },
        headers: { "X-CSRFTOKEN": this.csrf },
      }
    );
    expect(resp.status(), `createIssueRelation failed: ${resp.status()} ${await resp.text()}`).toBe(201);
  }
```

注意:

- `import { type APIRequestContext, expect, request as apiRequest } from "@playwright/test"` は既に存在する — 追加不要
- `import { env } from "./env"` は既に存在する — 追加不要
- `this.ctx.post` のシグネチャは既存の `createIssue` と完全に対称
- 戻り値は `void`（既存仕様で関係行のレスポンスボディを使う必要がない — Plan 06-02 はリレーション ID を参照しない）

**Smoke test:** Append the following test to `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` **inside the existing `test.describe`** block (一時的、Task 06-01-07 後に削除する候補):

```typescript
test("#smoke: relation seed survives deletion cascade", async ({ api }, testInfo) => {
  const suffix = `${testInfo.title.replace(/\s+/g, "-").slice(0, 40)}-${Date.now()}`;
  const [src, tgt] = await Promise.all([
    api.createIssue(`e2e-smoke-rel-src-${suffix}`, { start: 0, end: 3 }),
    api.createIssue(`e2e-smoke-rel-tgt-${suffix}`, { start: 5, end: 8 }),
  ]);
  try {
    // Should resolve to 201 (assertion is inside the helper)
    await api.createIssueRelation(src.id, tgt.id, "blocking");
  } finally {
    await Promise.allSettled([api.deleteIssue(src.id), api.deleteIssue(tgt.id)]);
  }
});
```

注意:

- このスモークは `api` フィクスチャだけを使う（`timeline` / `propagationTimeline` は使わない — Gantt ページに行かなくても API は叩ける）
- `try/finally` で teardown を保証する（`use()` の外で issues を作っているので fixture teardown には乗らない）
- スモークが GREEN になれば: (a) CSRF + storageState の reuse が機能、(b) URL ルーティングが正しい、(c) 201 レスポンスを受信

実行:

```bash
pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: relation seed"
```

期待: 1 passed.

Commit message:

```
feat(06-01): add Api.createIssueRelation helper + smoke test
```

  </action>
  <acceptance_criteria>
    - `apps/web/e2e/fixtures/api.ts` `grep -c 'async createIssueRelation' apps/web/e2e/fixtures/api.ts` returns `1`
    - `apps/web/e2e/fixtures/api.ts` `grep -c 'issue-relation/' apps/web/e2e/fixtures/api.ts` returns `1`
    - `apps/web/e2e/fixtures/api.ts` `grep -c 'X-CSRFTOKEN' apps/web/e2e/fixtures/api.ts` returns at least `3` (createIssue + deleteIssue + new method)
    - Existing methods byte-identical: `git diff apps/web/e2e/fixtures/api.ts` shows ONLY additions (no `-` lines for `createIssue` / `deleteIssue` / `createApi` / `dispose`)
    - Smoke test added: `grep -c '#smoke: relation seed' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - Verification command: `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: relation seed"` exits 0 with `1 passed`
  </acceptance_criteria>
  <verification>
    <command>pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: relation seed" 2>&amp;1 | tail -10</command>
    <expect>1 passed; exit code 0</expect>
  </verification>
</task>

<task id="06-01-03" wave="1" depends_on="[06-01-02]" autonomous="true">
  <description>Add `clearIssueDate` to `Api` class + smoke-test PATCH semantics.</description>
  <read_first>
    - apps/web/e2e/fixtures/api.ts (current state with createIssueRelation already added)
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"API Helper Recipes §2"
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Pitfall 9: clearIssueDate の HTTP status が期待と異なる"
    - .planning/phases/06-end-to-end-coverage-polish/06-PATTERNS.md §"`apps/web/e2e/fixtures/api.ts`" — `clearIssueDate` snippet
    - apps/web/e2e/specs/timeline-dependency-propagation.spec.ts (smoke spec target)
  </read_first>
  <action>
Append `clearIssueDate` method to the `Api` class in `apps/web/e2e/fixtures/api.ts` **after** `createIssueRelation`:

```typescript
  /**
   * Issue の start_date または target_date を null にクリアする(D-05)。
   *
   * URL: PATCH /api/workspaces/<slug>/projects/<projectId>/issues/<id>/
   * Body: { [field]: null }
   * Expected: 200 OK or 204 No Content (Plane の IssueViewSet.partial_update — smoke で確定)
   *
   * 用途: TEST-24 で tgt.target_date を null にし、サーバ側の伝播時に
   *      INCOMPLETE_SCHEDULE エラーを誘発する。
   *
   * 注意(D-07a / D-07b): この PATCH は server-side のみ変更し、ブラウザの
   *      ローカル MobX ストアには通知されない(WebSocket 購読なし)。よって
   *      ブラウザの isBlockComplete ガードは true のまま — ドラッグは発火する。
   */
  async clearIssueDate(issueId: string, field: "start_date" | "target_date"): Promise<void> {
    const resp = await this.ctx.patch(
      `/api/workspaces/${env.workspaceSlug}/projects/${env.projectId}/issues/${issueId}/`,
      {
        data: { [field]: null },
        headers: { "X-CSRFTOKEN": this.csrf },
      }
    );
    // Plane IssueViewSet.partial_update は通常 200 を返すが、204 でも許容
    expect(
      [200, 204],
      `clearIssueDate unexpected status: ${resp.status()} ${await resp.text()}`
    ).toContain(resp.status());
  }
```

**Smoke test:** Append to the existing `test.describe` in `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts`:

```typescript
test("#smoke: clearIssueDate sets target_date to null", async ({ api }, testInfo) => {
  const suffix = `${testInfo.title.replace(/\s+/g, "-").slice(0, 40)}-${Date.now()}`;
  const issue = await api.createIssue(`e2e-smoke-clear-${suffix}`, { start: 0, end: 7 });
  try {
    // Helper resolves with 200/204 — assertion inside helper
    await api.clearIssueDate(issue.id, "target_date");

    // Verify via getIssue (which is added in Task 06-01-04 — temporarily inline-call here
    // is rejected; instead assert the PATCH succeeded by verifying the next createIssue+drag
    // doesn't include this issue. For Task 06-01-03 we only assert the helper does not throw).
  } finally {
    await api.deleteIssue(issue.id);
  }
});
```

注意:

- このスモークは厳密には PATCH が成功したかしか検証しない。`null` が永続化されたかは Task 06-01-04 で `getIssue` を追加してから cross-check する。Task 06-01-03 のスコープは PATCH ステータスコードの確定のみ。
- 既知の不確実性 ([ASSUMED] in RESEARCH §A1): Plane が `target_date: null` を受け付けない場合、helper が non-2xx で fail し別の INCOMPLETE_SCHEDULE トリガー方法を要する。Smoke で確定する。
- 実際に観測した status code (200 or 204) を Task 06-01 の SUMMARY にメモする — Plan 06-02 の TEST-24 でも同じ helper を使うため。

実行:

```bash
pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: clearIssueDate"
```

期待: 1 passed.

万一 PATCH が 400/422 で fail した場合: Plane の `IssueSerializer` が `target_date` の null 化を拒否している可能性。fallback 戦略:

1. PATCH body に `target_date: ""` を試す
2. `start_date` をクリアして INCOMPLETE_SCHEDULE をトリガーする (target → start に役割スワップ)
3. それでも fail なら issue を削除して新規 `target_date` 未指定の issue を作り、関係再シードで対応 — ただしこれは TEST-24 の seeding が複雑化するため最終手段

ただし fallback が必要になるのは [ASSUMED] が崩れた場合のみ。最初に 200/204 を期待。

Commit message:

```
feat(06-01): add Api.clearIssueDate helper + smoke test
```

  </action>
  <acceptance_criteria>
    - `apps/web/e2e/fixtures/api.ts` `grep -c 'async clearIssueDate' apps/web/e2e/fixtures/api.ts` returns `1`
    - `apps/web/e2e/fixtures/api.ts` `grep -c '\[200, 204\]' apps/web/e2e/fixtures/api.ts` returns `1`
    - `apps/web/e2e/fixtures/api.ts` contains `this.ctx.patch` exactly once: `grep -c 'this.ctx.patch' apps/web/e2e/fixtures/api.ts` returns `1`
    - Existing methods byte-identical: `git diff apps/web/e2e/fixtures/api.ts` shows ONLY additions in this commit's diff
    - Smoke test added: `grep -c '#smoke: clearIssueDate' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - Verification command: `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: clearIssueDate"` exits 0 with `1 passed`
    - Document the actual observed status code (200 or 204) in the commit body
  </acceptance_criteria>
  <verification>
    <command>pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: clearIssueDate" 2>&amp;1 | tail -10</command>
    <expect>1 passed; exit code 0</expect>
  </verification>
</task>

<task id="06-01-04" wave="1" depends_on="[06-01-03]" autonomous="true">
  <description>Add `getIssue` to `Api` class + smoke-test response shape and stale-cache absence.</description>
  <read_first>
    - apps/web/e2e/fixtures/api.ts (current state with createIssueRelation + clearIssueDate)
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"API Helper Recipes §3"
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Persistence Read Path"
    - .planning/phases/06-end-to-end-coverage-polish/06-PATTERNS.md §"`apps/web/e2e/fixtures/api.ts`" — `getIssue` snippet
  </read_first>
  <action>
Append `getIssue` method to the `Api` class in `apps/web/e2e/fixtures/api.ts` **after** `clearIssueDate`:

```typescript
  /**
   * Issue を 1 件取得する(D-05)。
   *
   * URL: GET /api/workspaces/<slug>/projects/<projectId>/issues/<id>/
   * Expected: 200 OK
   * Returns: CreatedIssue 型(部分集合 — GET レスポンスは追加フィールドを持つが
   *          test 側は id / start_date / target_date のみ参照する)。
   *
   * 用途: TEST-23 で tgt.start_date / target_date が伝播後に
   *      bulk_update で永続化されたことを直接 DB から読み出して確認する。
   *      DRF の retrieve は Redis キャッシュを使わない(RESEARCH §Persistence Read Path)
   *      ため stale read のリスクなし。
   */
  async getIssue(issueId: string): Promise<CreatedIssue> {
    const resp = await this.ctx.get(
      `/api/workspaces/${env.workspaceSlug}/projects/${env.projectId}/issues/${issueId}/`,
      { headers: { "X-CSRFTOKEN": this.csrf } }
    );
    expect(resp.status(), `getIssue failed: ${resp.status()} ${await resp.text()}`).toBe(200);
    return (await resp.json()) as CreatedIssue;
  }
```

**Smoke test:** Replace the placeholder smoke from Task 06-01-03 (the one that did not verify the null persistence) with a stronger version that uses `getIssue`. Append the following to `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts`:

```typescript
test("#smoke: getIssue reads back created dates and clearIssueDate persists null", async ({ api }, testInfo) => {
  const suffix = `${testInfo.title.replace(/\s+/g, "-").slice(0, 40)}-${Date.now()}`;
  const issue = await api.createIssue(`e2e-smoke-get-${suffix}`, { start: 0, end: 7 });
  try {
    // Initial read — should have both dates populated
    const initial = await api.getIssue(issue.id);
    // Use type assertion for null-checking (Date strings are always truthy)
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    // (guard against undefined; Plane returns ISO date strings)
    // expect.toBeTruthy() catches both null and "" but allows "2026-05-04"
    // expect(initial.start_date).toBeTruthy();
    // expect(initial.target_date).toBeTruthy();
    // 注: createIssue が 201 を返した時点で dates が populated なのは保証されているので
    // ここでは「helper が CreatedIssue を返した」だけ確認すれば良い。
    if (!initial.start_date || !initial.target_date) {
      throw new Error(
        `getIssue smoke: initial dates missing — start=${initial.start_date} target=${initial.target_date}`
      );
    }

    // Clear target_date — server-side null
    await api.clearIssueDate(issue.id, "target_date");

    // Re-read — target_date must now be null (stale cache check)
    const after = await api.getIssue(issue.id);
    if (after.target_date !== null) {
      throw new Error(`getIssue smoke: clearIssueDate did not persist null — target_date=${after.target_date}`);
    }
  } finally {
    await api.deleteIssue(issue.id);
  }
});
```

注意:

- `expect()` は `import` していないため、生の `throw` で fail させる（既存スペックは `expect` をインポートしているが、ここでは依存を最小化）
- もし `expect` を使いたい場合は `import { test, expect } from "../fixtures/test-fixtures"` に変更（既存パターン）— **既存の `import { test } from "../fixtures/test-fixtures"` を変更するのは OK** だが、変更時は他のスモークテストでも `expect` が使えるようにすること
- 推奨: `import { test, expect } from "../fixtures/test-fixtures"` に変更し、`throw new Error` を `expect(initial.start_date).toBeTruthy()` 等に置き換える方が読みやすい
- `getIssue` の戻り型 `CreatedIssue` は `start_date` / `target_date` を non-null で型定義しているが、PATCH 後は null が返る — TypeScript 的には型不一致だが、runtime 検証なので OK。Plan 06-02 で TypeScript 厳密性が問題になる場合は `CreatedIssue` の型を `start_date: string | null; target_date: string | null` に緩める（ただし既存テスト 3 件には影響しない — `createIssue` 直後はどちらも non-null）

**[ASSUMED] 検証**: スモーク GREEN が以下を確認する:

- A1 (clearIssueDate が null を受け付ける): 確認
- A2 (getIssue が CreatedIssue 互換のフィールドを返す): 確認
- A3 (Redis キャッシュなし、PATCH 直後に null が返る): 確認

実行:

```bash
pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: getIssue"
```

期待: 1 passed.

Commit message:

```
feat(06-01): add Api.getIssue helper + smoke test verifying clearIssueDate null-persistence
```

  </action>
  <acceptance_criteria>
    - `apps/web/e2e/fixtures/api.ts` `grep -c 'async getIssue' apps/web/e2e/fixtures/api.ts` returns `1`
    - `apps/web/e2e/fixtures/api.ts` contains `this.ctx.get(` exactly twice (CSRF endpoint + getIssue): `grep -c 'this.ctx.get(' apps/web/e2e/fixtures/api.ts` returns `2`. Note: createApi already uses `context.get("/auth/get-csrf-token/")` (different `context`, not `this.ctx`), so `this.ctx.get` is `1` for getIssue; the assertion: `grep -c 'this.ctx.get(' apps/web/e2e/fixtures/api.ts` returns `1`. Use this stricter form.
    - Smoke test added: `grep -c '#smoke: getIssue' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - Existing methods byte-identical: `git diff apps/web/e2e/fixtures/api.ts` shows ONLY additions in this commit
    - Verification command: `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: getIssue"` exits 0 with `1 passed`
  </acceptance_criteria>
  <verification>
    <command>pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: getIssue" 2>&amp;1 | tail -10</command>
    <expect>1 passed; exit code 0</expect>
  </verification>
</task>

<task id="06-01-05" wave="1" depends_on="[06-01-04]" autonomous="true">
  <description>Add `getBlockBox` + `getDayWidthFromBlock` + `dragBlockBy` methods to `TimelinePage` POM, then smoke-test that block dragging shifts the bounding box.</description>
  <read_first>
    - apps/web/e2e/pages/timeline.page.ts (current state — `block(issueId)`, `waitForBlock`, DRAG_STEPS, dispatchEvent fallback pattern)
    - apps/web/core/components/gantt-chart/blocks/block.tsx (lines 99-117 — `data-block-id`, `(daysDiff + 1) * dayWidth` formula)
    - apps/web/core/components/gantt-chart/helpers/draggable.tsx (line 61 — onMouseDown wire)
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Drag Mechanics"
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"DOM Derivation Recipes"
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Pitfall 1: page.mouse.down() が React onMouseDown を miss する"
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Pitfall 3: dayWidth 計算の off-by-one"
    - .planning/phases/06-end-to-end-coverage-polish/06-PATTERNS.md §"`apps/web/e2e/pages/timeline.page.ts`"
  </read_first>
  <action>
Add three new methods to `TimelinePage` in `apps/web/e2e/pages/timeline.page.ts`. Insert them **after** `clickPickerOption` (the last existing method) — keep all existing methods byte-identical.

```typescript
  /**
   * ブロックのバウンディングボックスを取得する(D-03)。
   *
   * hover を挟むことで RenderIfVisible が viewport 外でも stable になる。
   * 用途: TEST-23 / TEST-24 の pre-drag / post-drag 位置キャプチャ。
   */
  async getBlockBox(issueId: string): Promise<{ x: number; y: number; width: number; height: number }> {
    await this.block(issueId).hover();
    const box = await this.block(issueId).boundingBox();
    if (!box) {
      throw new Error(`block ${issueId} has no bounding box (off-screen or zero-sized)`);
    }
    return box;
  }

  /**
   * DOM レンダリングから dayWidth を導出する(D-03a)。
   *
   * 公式: block.tsx の getPositionFromDateOnGantt は (daysDiff + 1) * dayWidth を使う
   *       (start_date 当日も含む inclusive count)。よって:
   *       dayCount = (target_date - start_date) calendar days + 1
   *       dayWidth = boundingBox.width / dayCount
   *
   * 注: chart-coords.ts を import しない(D-03b)— DOM 派生のみ。
   */
  async getDayWidthFromBlock(
    issueId: string,
    issue: { start_date: string; target_date: string }
  ): Promise<number> {
    const box = await this.block(issueId).boundingBox();
    if (!box) {
      throw new Error(`block ${issueId} has no bounding box (off-screen or zero-sized)`);
    }
    const start = new Date(issue.start_date);
    const end = new Date(issue.target_date);
    // (daysDiff + 1) inclusive count — block.tsx:99-106 と一致
    const dayCount = Math.round((end.getTime() - start.getTime()) / (1000 * 60 * 60 * 24)) + 1;
    if (dayCount <= 0) {
      throw new Error(
        `getDayWidthFromBlock: invalid date range (start=${issue.start_date}, target=${issue.target_date})`
      );
    }
    return box.width / dayCount;
  }

  /**
   * ブロック本体をドラッグして deltaDays 日分だけ右に移動させる(D-03 / D-12)。
   *
   * - centerX/Y: ブロック本体の中央(data-block-id の bounding box 中央)
   * - draggable.tsx:61 の onMouseDown は pointer-events:none ではないため
   *   native page.mouse.down() で発火できる(D-12 第一選択)。
   *   万一 React 合成イベントが発火しない場合は dispatchEvent fallback に切り替える(下記コメント参照)。
   * - DRAG_STEPS = 20 で既存の handle-drag と同じ歩数で動かす。
   *
   * 引数 issue: dayWidth 算出のため start_date / target_date を受け取る(D-03a — DOM 派生)。
   */
  async dragBlockBy(
    issueId: string,
    deltaDays: number,
    issue: { start_date: string; target_date: string }
  ): Promise<void> {
    await this.block(issueId).hover();

    const box = await this.block(issueId).boundingBox();
    if (!box) {
      throw new Error(`block ${issueId} has no bounding box (off-screen or zero-sized)`);
    }

    const centerX = box.x + box.width / 2;
    const centerY = box.y + box.height / 2;

    const dayWidth = await this.getDayWidthFromBlock(issueId, issue);
    const pixelDelta = deltaDays * dayWidth;

    // D-12: native mouse.down を第一選択(ブロックボディは pointer-events:auto)
    // fallback (smoke で発火しない場合): handle.dispatchEvent("mousedown", { ... }) パターン
    //   apps/web/e2e/pages/timeline.page.ts:70-81 の startDragFromEdge 参照
    await this.page.mouse.move(centerX, centerY);
    await this.page.mouse.down();

    await this.page.mouse.move(centerX + pixelDelta, centerY, { steps: DRAG_STEPS });

    await this.page.mouse.up();
  }
```

**Smoke test:** Append to the existing `test.describe` in `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts`. このスモークでは relation を作らず単独 issue をドラッグするだけ — sibling 伝播は発生しないが、`src` 自身の DOM 移動を確認できる。

```typescript
test("#smoke: dragBlockBy moves the dragged block visually (no relation)", async ({ api, page }, testInfo) => {
  const { TimelinePage } = await import("../pages/timeline.page");
  const suffix = `${testInfo.title.replace(/\s+/g, "-").slice(0, 40)}-${Date.now()}`;
  const issue = await api.createIssue(`e2e-smoke-drag-${suffix}`, { start: 0, end: 3 });
  try {
    const tp = new TimelinePage(page);
    await tp.gotoIssueGantt();
    await tp.waitForBlock(issue.id);

    const preBox = await tp.getBlockBox(issue.id);

    // 単独 issue を 4 日右にドラッグ — server は 200 (no propagation needed) を返す
    // 注: relation がないため伝播エンドポイントが返すのは src 単独の更新
    const responsePromise = page.waitForResponse(
      (r) => r.url().includes("/timeline-propagation/") && r.request().method() === "POST",
      { timeout: 15_000 }
    );
    await tp.dragBlockBy(issue.id, 4, issue);
    const resp = await responsePromise;
    // ステータスは 200 (no boundary violation で伝播も発火する)。relation がない場合の挙動は
    // Phase 5 の base-gantt-root の D-01 split に依存 — ここではステータスが 200 か 422 のいずれかを許容。
    // 実際: relation なし → propagationStore.commitWithServerResult が呼ばれるが、伝播対象は dragged のみ
    //       → サーバは 200 を返し total_updated_count = 1
    if (resp.status() !== 200) {
      throw new Error(`smoke drag: expected 200, got ${resp.status()} body=${await resp.text()}`);
    }

    // DOM が動いたこと(MobX flush 後)を expect.poll で確認 — D-12b
    // Drag 後の box.x は preBox.x + pixelDelta(±tolerance)
    const dayWidth = await tp.getDayWidthFromBlock(issue.id, issue);
    // server がドラッグ後の position に再配置 — block.position から計算される final box.x は
    // preBox.x + 4 * dayWidth(server から返った start_date が +4 されたため)
    const { expect } = await import("../fixtures/test-fixtures");
    await expect
      .poll(async () => (await tp.getBlockBox(issue.id)).x, { timeout: 5_000 })
      .toBeGreaterThan(preBox.x + dayWidth - 2);
  } finally {
    await api.deleteIssue(issue.id);
  }
});
```

注意:

- このスモークは `import("../pages/timeline.page")` と `import("../fixtures/test-fixtures")` を **dynamic import** で行う。これは spec の top-level import を最小に保つため(既存スペックが `import { test } from "../fixtures/test-fixtures"` のみで `expect` も `TimelinePage` も import していない場合の互換措置)。
- もし top-level に既に `import { test, expect } from "../fixtures/test-fixtures"` がある(Task 06-01-04 で変更した場合)、dynamic import は不要 — `expect` を直接使える。同様に `TimelinePage` も top-level import に追加して良い。
- スモーク GREEN が以下を確認する:
  - (a) `dragBlockBy` が React `onMouseDown` を発火させた(D-12 native パスが機能)
  - (b) `dayWidth` 計算が正しい(off-by-one なし)
  - (c) サーバが 200 を返し block の DOM 位置が更新された

**もし `page.mouse.down()` が `onMouseDown` を発火しない場合**(Pitfall 1 発生): `dragBlockBy` の native パス部分を以下で置き換える:

```typescript
// dispatchEvent fallback (handle-drag の startDragFromEdge と対称)
await this.block(issueId).dispatchEvent("mousedown", {
  button: 0,
  buttons: 1,
  clientX: centerX,
  clientY: centerY,
  bubbles: true,
  cancelable: true,
});
await this.page.mouse.move(centerX, centerY);
```

スモークが GREEN になるまで native → dispatchEvent → ハイブリッド の順で試行。最終的に GREEN になったパスを `dragBlockBy` に commit する。

実行:

```bash
pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: dragBlockBy"
```

期待: 1 passed.

Commit message:

```
feat(06-01): add TimelinePage.{getBlockBox,getDayWidthFromBlock,dragBlockBy} + smoke test
```

  </action>
  <acceptance_criteria>
    - `apps/web/e2e/pages/timeline.page.ts` `grep -c 'async getBlockBox' apps/web/e2e/pages/timeline.page.ts` returns `1`
    - `apps/web/e2e/pages/timeline.page.ts` `grep -c 'async getDayWidthFromBlock' apps/web/e2e/pages/timeline.page.ts` returns `1`
    - `apps/web/e2e/pages/timeline.page.ts` `grep -c 'async dragBlockBy' apps/web/e2e/pages/timeline.page.ts` returns `1`
    - `apps/web/e2e/pages/timeline.page.ts` contains `(end.getTime() - start.getTime()) / (1000 * 60 * 60 * 24)) + 1` (dayCount inclusive formula): `grep -c '+ 1' apps/web/e2e/pages/timeline.page.ts` returns at least `1` (could match other lines too — qualitative check ok)
    - `apps/web/e2e/pages/timeline.page.ts` does NOT import `chart-coords` (D-03b): `grep -c 'chart-coords' apps/web/e2e/pages/timeline.page.ts` returns `0`
    - Existing `block` / `waitForBlock` / `dragRightTo` / `dragLeftTo` / `startDragFromEdge` / `dropOnEdge` / `picker` / `clickPickerOption` byte-identical: `git diff apps/web/e2e/pages/timeline.page.ts` shows ONLY additions
    - Smoke test added: `grep -c '#smoke: dragBlockBy' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - Verification command: `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: dragBlockBy"` exits 0 with `1 passed`
    - If native `page.mouse.down()` did not work and dispatchEvent fallback was chosen, document the choice in the commit body and reference Pitfall 1
  </acceptance_criteria>
  <verification>
    <command>pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: dragBlockBy" 2>&amp;1 | tail -20</command>
    <expect>1 passed; exit code 0</expect>
  </verification>
</task>

<task id="06-01-06" wave="1" depends_on="[06-01-05]" autonomous="true">
  <description>Add `propagationPair` + `propagationTimeline` fixtures to `test-fixtures.ts` + smoke-test cascade-on-delete and rendered-block availability.</description>
  <read_first>
    - apps/web/e2e/fixtures/test-fixtures.ts (current state — `Fixtures` type, `base.extend`, `Promise.allSettled` teardown)
    - .planning/phases/06-end-to-end-coverage-polish/06-CONTEXT.md §"D-06"
    - .planning/phases/06-end-to-end-coverage-polish/06-CONTEXT.md §"D-06b" — day spacing
    - .planning/phases/06-end-to-end-coverage-polish/06-CONTEXT.md §"D-05b" — cascade
    - .planning/phases/06-end-to-end-coverage-polish/06-PATTERNS.md §"`apps/web/e2e/fixtures/test-fixtures.ts`"
    - apps/web/e2e/fixtures/api.ts (with all helpers from 06-01-02..04)
    - apps/web/e2e/pages/timeline.page.ts (with `dragBlockBy` etc. from 06-01-05)
  </read_first>
  <action>
Modify `apps/web/e2e/fixtures/test-fixtures.ts` to add `propagationPair` and `propagationTimeline` to the `Fixtures` type and the `base.extend` block. **Existing `api` / `issuePair` / `timeline` fixtures must stay byte-identical**.

Final state of `apps/web/e2e/fixtures/test-fixtures.ts`:

```typescript
import { test as base } from "@playwright/test";
import { Api, createApi, type CreatedIssue } from "./api";
import { TimelinePage } from "../pages/timeline.page";

type Fixtures = {
  api: Api;
  /** Issue を 2 つ作成した状態(src / tgt)でテストに渡す。afterEach で自動削除。 */
  issuePair: { src: CreatedIssue; tgt: CreatedIssue };
  /** Gantt 画面へ遷移した TimelinePage POM。`issuePair` に依存して構築される。 */
  timeline: TimelinePage;
  /**
   * Phase 6 (D-06): src→tgt の blocking リレーションが既にシードされた状態で
   * 2 つの Issue を渡す。propagationTimeline と組み合わせて TEST-23 / TEST-24 で使用。
   *
   * Day spacing (D-06b): src start+0/end+3, tgt start+5/end+8 — 2-day gap, 3-day duration each.
   * これにより src を +4 日右にドラッグすると src.target=+7 が tgt.start=+5 を超え、
   * boundary violation で tgt が右シフトされる。
   *
   * Cleanup: IssueRelation.issue は ON DELETE CASCADE のため src 削除でリレーション行も自動除去。
   */
  propagationPair: { src: CreatedIssue; tgt: CreatedIssue };
  /** Gantt 画面へ遷移した TimelinePage POM。`propagationPair` に依存。 */
  propagationTimeline: TimelinePage;
};

export const test = base.extend<Fixtures>({
  // eslint-disable-next-line no-empty-pattern
  api: async ({}, use) => {
    const api = await createApi();
    await use(api);
    await api.dispose();
  },

  issuePair: async ({ api }, use, testInfo) => {
    // テスト名にユニーク suffix を付けて識別しやすく
    const suffix = `${testInfo.title.replace(/\s+/g, "-").slice(0, 40)}-${Date.now()}`;
    const [src, tgt] = await Promise.all([
      api.createIssue(`e2e-src-${suffix}`, { start: 0, end: 3 }),
      api.createIssue(`e2e-tgt-${suffix}`, { start: 4, end: 7 }),
    ]);

    await use({ src, tgt });

    // 失敗テストでも確実に cleanup(afterEach 相当)
    await Promise.allSettled([api.deleteIssue(src.id), api.deleteIssue(tgt.id)]);
  },

  timeline: async ({ page, issuePair }, use) => {
    const tp = new TimelinePage(page);
    await tp.gotoIssueGantt();
    await tp.waitForBlock(issuePair.src.id);
    await tp.waitForBlock(issuePair.tgt.id);
    await use(tp);
  },

  propagationPair: async ({ api }, use, testInfo) => {
    // D-06b: src start+0/end+3, tgt start+5/end+8 — 2-day gap で boundary violation を確実に発生させる
    const suffix = `${testInfo.title.replace(/\s+/g, "-").slice(0, 40)}-${Date.now()}`;
    const [src, tgt] = await Promise.all([
      api.createIssue(`e2e-prop-src-${suffix}`, { start: 0, end: 3 }),
      api.createIssue(`e2e-prop-tgt-${suffix}`, { start: 5, end: 8 }),
    ]);

    // blocking リレーションを API 経由でシード(D-06: 関係作成は UI ドラッグではなくセットアップ)
    await api.createIssueRelation(src.id, tgt.id, "blocking");

    await use({ src, tgt });

    // IssueRelation は ON DELETE CASCADE なので src 削除で自動的に除去される(D-05b)
    await Promise.allSettled([api.deleteIssue(src.id), api.deleteIssue(tgt.id)]);
  },

  propagationTimeline: async ({ page, propagationPair }, use) => {
    const tp = new TimelinePage(page);
    await tp.gotoIssueGantt();
    await tp.waitForBlock(propagationPair.src.id);
    await tp.waitForBlock(propagationPair.tgt.id);
    await use(tp);
  },
});

export { expect } from "@playwright/test";
```

注意:

- `api` / `issuePair` / `timeline` の definition は既存と完全一致 — `git diff` で `-` 行が出ないこと
- 順序は: `api` → `issuePair` → `timeline` → `propagationPair` → `propagationTimeline`(末尾追加)

**Smoke test:** Append to the existing `test.describe` in `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts`:

```typescript
test("#smoke: propagationPair seeds relation and propagationTimeline renders both blocks", async ({
  api,
  propagationPair,
  propagationTimeline,
}) => {
  const { src, tgt } = propagationPair;

  // propagationTimeline fixture 構築直後 → 両ブロックが DOM にいる(waitForBlock を経由)
  const srcBox = await propagationTimeline.getBlockBox(src.id);
  const tgtBox = await propagationTimeline.getBlockBox(tgt.id);

  if (srcBox.width <= 0 || tgtBox.width <= 0) {
    throw new Error(`smoke fixture: zero-width blocks — src.width=${srcBox.width} tgt.width=${tgtBox.width}`);
  }

  // tgt は src の右側にあるはず (D-06b: src end=+3, tgt start=+5 → tgt.x > src.x + src.width)
  if (tgtBox.x <= srcBox.x + srcBox.width) {
    throw new Error(
      `smoke fixture: tgt block is not to the right of src — src.x=${srcBox.x} src.width=${srcBox.width} tgt.x=${tgtBox.x}`
    );
  }

  // src→tgt の blocking リレーションが描画されている (line via dependency-paths.tsx)
  // 既存の Phase 1 specs 同様の data-key で確認
  // 注: Phase 1 spec line 22 と一致 — `${src.id}-blocking-${tgt.id}`
  const { expect } = await import("../fixtures/test-fixtures");
  await expect(propagationTimeline.block(src.id)).toBeVisible();
  await expect(propagationTimeline.block(tgt.id)).toBeVisible();

  // optional: 関係線が描画されているか(propagationPair が API 経由で関係を作っているので必ず存在)
  await expect(
    propagationTimeline.block(src.id).page().locator(`[data-dependency-key="${src.id}-blocking-${tgt.id}"]`)
  ).toBeVisible({ timeout: 5_000 });
});
```

注意:

- このスモークは `propagationPair` + `propagationTimeline` の両方を消費する — fixture 連鎖が正しく機能することを確認
- `propagationTimeline.block(src.id).page()` は Playwright Locator から page を辿るパターン — 既存スペックでは `page` を直接 destructure しているが、ここでは fixture 連鎖の自己完結を優先
- 代替: `test(..., async ({ propagationTimeline, propagationPair, page }) => { ... await expect(page.locator(...)) ... })` の形で `page` も destructure すれば良い(より直接的)— こちらを推奨

実行:

```bash
pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: propagationPair"
```

期待: 1 passed.

Commit message:

```
feat(06-01): add propagationPair + propagationTimeline fixtures + smoke test
```

  </action>
  <acceptance_criteria>
    - `apps/web/e2e/fixtures/test-fixtures.ts` `grep -c 'propagationPair:' apps/web/e2e/fixtures/test-fixtures.ts` returns `2` (type definition + base.extend entry)
    - `apps/web/e2e/fixtures/test-fixtures.ts` `grep -c 'propagationTimeline:' apps/web/e2e/fixtures/test-fixtures.ts` returns `2`
    - `apps/web/e2e/fixtures/test-fixtures.ts` `grep -c "createIssueRelation" apps/web/e2e/fixtures/test-fixtures.ts` returns `1` (called in propagationPair seeding)
    - `apps/web/e2e/fixtures/test-fixtures.ts` `grep -c 'start: 5, end: 8' apps/web/e2e/fixtures/test-fixtures.ts` returns `1` (D-06b tgt spacing)
    - Existing `api` / `issuePair` / `timeline` fixtures byte-identical: `git log -1 -p apps/web/e2e/fixtures/test-fixtures.ts | grep -c '^-' | awk '{ if ($1 <= 1) exit 0; else exit 1 }'` (≤1 deletion to allow for trailing-newline whitespace edits — strict equality also acceptable)
    - Smoke test added: `grep -c '#smoke: propagationPair' apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` returns `1`
    - Verification command: `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: propagationPair"` exits 0 with `1 passed`
  </acceptance_criteria>
  <verification>
    <command>pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke: propagationPair" 2>&amp;1 | tail -15</command>
    <expect>1 passed; exit code 0</expect>
  </verification>
</task>

<task id="06-01-07" wave="1" depends_on="[06-01-06]" autonomous="true">
  <description>Regression + lint + types gate: confirm existing 3 relation-creation specs still pass, OxLint budget unchanged, TypeScript clean. Smoke tests are kept (deletion is Plan 06-02's responsibility).</description>
  <read_first>
    - apps/web/e2e/specs/timeline-dependency-drag.spec.ts (existing 3 specs — must not regress)
    - apps/web/e2e/specs/timeline-dependency-propagation.spec.ts (newly built — 5 smoke + 2 placeholders)
    - CLAUDE.md (OxLint budget rule — apps/web=11957, ratcheting)
    - .planning/phases/06-end-to-end-coverage-polish/06-CONTEXT.md §"D-10"
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Pitfall 8: 既存 3 スペックの regression"
    - .planning/phases/06-end-to-end-coverage-polish/06-RESEARCH.md §"Pitfall 10: OxLint 警告バジェット超過"
  </read_first>
  <action>
3-step gate:

**Step 1 — Existing 3 specs regression check:**

```bash
pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "timeline dependency drag"
```

期待: 3 passed (Phase 1 が出した既存 3 specs)。0 failed。

もし fail したら:

- どの spec が壊れたか特定
- 原因が `test-fixtures.ts` の `Fixtures` 型変更にあるか、`timeline.page.ts` のメソッド追加にあるかを切り分け
- 既存メソッドが byte-identical であることを `git diff HEAD~6 apps/web/e2e/pages/timeline.page.ts apps/web/e2e/fixtures/test-fixtures.ts apps/web/e2e/fixtures/api.ts | grep "^-"` で確認(コメント・空白以外の `-` 行があれば回帰要因)

**Step 2 — OxLint budget gate:**

```bash
pnpm --filter=web check:lint 2>&1 | tail -20
```

期待: `--max-warnings 11957` に対して 11957 以下の警告。既存ベースラインは Plan 05-02 完了時点で 995 — Plan 06-01 で追加した E2E ファイルは **0 warnings** で通過するはず(D-10)。

ベースライン記録:

```bash
pnpm --filter=web check:lint 2>&1 | grep -E "(warnings|error|max-warnings)" | tail -5
```

もし新規警告が出たら:

- 警告ルール名と該当行を特定
- 既存スペック (`timeline-dependency-drag.spec.ts`) が同様のコードで警告を出していないか確認
- 出ていなければ直す。出ているなら(既存ベースラインが許容しているということ) `oxlint-disable-next-line <rule>` で local 抑制を許容(極力避ける — まずは修正)

**Step 3 — TypeScript types gate:**

```bash
pnpm --filter=web check:types 2>&1 | tail -20
```

期待: 0 errors。

もし `CreatedIssue` の `start_date: string | null` 不一致で type error が出たら:

- option A: `apps/web/e2e/fixtures/api.ts` の `CreatedIssue` を `start_date: string | null; target_date: string | null` に変更(既存のスモーク + Phase 1 specs が non-null を仮定しているか確認)
- option B: `getIssue` の戻り型を `Promise<CreatedIssue & { start_date: string | null; target_date: string | null }>` で部分緩和
- 推奨: option A — 既存スペックは PATCH で null 化していないので影響なし

**Step 4 — Full E2E suite (idempotency check):**

```bash
pnpm --filter=web test:e2e 2>&1 | tail -30
```

期待:

- 既存 3 specs (timeline dependency drag #1/#2/#3) → 3 passed
- 新規 4 smoke specs (relation seed / clearIssueDate / getIssue / dragBlockBy / propagationPair) → 5 passed
- 2 placeholder skipped (TEST-23 / TEST-24)
- Total: 8 passed + 2 skipped, 0 failed

**Step 5 — Document Plan 06-01 outcome in commit body**:

- 観測した `clearIssueDate` のステータスコード(200 or 204)
- `dragBlockBy` で native `page.mouse.down()` が成功したか、dispatchEvent fallback が必要だったか
- ベースライン警告数の差分

Plan 06-01 はここで終了。Plan 06-02 は Wave 2 で TEST-23 / TEST-24 の実装を行う(その時点で smoke スペックを `test.skip(...)` に変えるか削除するかは Plan 06-02 の責務)。

Commit message:

```
test(06-01): regression + lint + types gate; document Plan 06-01 outcome

- 既存 3 specs (timeline dependency drag) 全て GREEN
- OxLint warnings: <BEFORE> → <AFTER> (budget 11957 / observed)
- TypeScript: 0 errors
- 新規 5 smoke specs all GREEN; 2 TEST placeholder skipped
- clearIssueDate status: 200 [or 204]
- dragBlockBy mousedown: native page.mouse.down() [or dispatchEvent fallback]
```

  </action>
  <acceptance_criteria>
    - Verification command 1: `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "timeline dependency drag"` exits 0 with `3 passed`
    - Verification command 2: `pnpm --filter=web check:lint` exits 0; warning count ≤ 11957 (OxLint budget unchanged)
    - Verification command 3: `pnpm --filter=web check:types` exits 0
    - Verification command 4: `pnpm --filter=web test:e2e` exits 0 with full suite GREEN (≥ 8 passed; 0 failed; ≤ 2 skipped)
    - Commit body documents (a) `clearIssueDate` observed HTTP status, (b) `dragBlockBy` mousedown variant chosen, (c) lint baseline diff
    - No production code modified: `git diff HEAD~7 -- apps/web/core apps/web/ce` produces empty output (or check via `git diff origin/feature/timeline-dependency-drag -- apps/web/core apps/web/ce` if branch is base)
  </acceptance_criteria>
  <verification>
    <command>pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "timeline dependency drag" 2>&amp;1 | tail -10 &amp;&amp; pnpm --filter=web check:lint 2>&amp;1 | tail -5 &amp;&amp; pnpm --filter=web check:types 2>&amp;1 | tail -5</command>
    <expect>3 passed (existing specs); 0 lint failures; 0 type errors</expect>
  </verification>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                                                                                                          |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Playwright APIRequestContext → Django API | Test seeds (`createIssueRelation`, `clearIssueDate`) flow through the same `session-id` cookie + CSRF token as production calls; storageState is reused across tests within a worker |
| Playwright browser → Django API           | `dragBlockBy` triggers production drag → propagation endpoint → DB writes via the production wire                                                                                    |
| Test data → Production tables             | Each test creates issues + relations in the dev DB; teardown via `Promise.allSettled` ensures cleanup; cascade-on-delete prevents orphans                                            |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                                           | Disposition | Mitigation Plan                                                                                                                                                               |
| ---------- | -------------------------- | --------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-01-01 | T (Tampering)              | `apps/web/e2e/fixtures/api.ts::createIssueRelation` | mitigate    | CSRF token included via `X-CSRFTOKEN` header (existing pattern); session-cookie auth via `storageState`                                                                       |
| T-06-01-02 | T (Tampering)              | `apps/web/e2e/fixtures/api.ts::clearIssueDate`      | mitigate    | CSRF + session-cookie auth; PATCH targets the dev workspace's `E2E_PROJECT_ID` only — no path traversal                                                                       |
| T-06-01-03 | I (Information disclosure) | Test data persistence                               | accept      | Tests run against local dev stack only (D-08); no real PII; `Promise.allSettled` teardown removes test-created rows                                                           |
| T-06-01-04 | D (Denial of service)      | OxLint warning budget overflow                      | mitigate    | Plan 06-01 Task 06-01-07 enforces `pnpm --filter=web check:lint` budget = 11957                                                                                               |
| T-06-01-05 | E (Elevation of privilege) | Cross-test fixture leakage                          | mitigate    | Playwright `workers: 1` + `fullyParallel: false` (existing config) ensures no concurrent test interference; per-test unique suffix on issue names; cascade delete on teardown |

</threat_model>

<verification>
End-of-Plan-06-01 checks:

- [ ] `apps/web/e2e/specs/timeline-dependency-propagation.spec.ts` exists (placeholder + 4 smoke + 2 TEST stubs)
- [ ] `apps/web/e2e/fixtures/api.ts` has `createIssueRelation`, `clearIssueDate`, `getIssue` (existing methods byte-identical)
- [ ] `apps/web/e2e/fixtures/test-fixtures.ts` has `propagationPair`, `propagationTimeline` in `Fixtures` type and `base.extend` (existing fixtures byte-identical)
- [ ] `apps/web/e2e/pages/timeline.page.ts` has `getBlockBox`, `getDayWidthFromBlock`, `dragBlockBy` (existing methods byte-identical)
- [ ] `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "timeline dependency drag"` → 3 passed (regression guard)
- [ ] `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "smoke"` → 4–5 passed
- [ ] `pnpm --filter=web exec playwright test --config=e2e/playwright.config.ts --grep "TEST-23"` → at least 1 skipped (placeholder still present)
- [ ] `pnpm --filter=web check:lint` budget 11957 honored
- [ ] `pnpm --filter=web check:types` clean
- [ ] No `apps/web/core/` or `apps/web/ce/` modifications
- [ ] No `playwright.config.ts` / `auth.setup.ts` / `package.json` / `turbo.json` / `.gitignore` modifications
      </verification>

<success_criteria>
Plan 06-01 ships when:

- All 7 tasks committed atomically (one commit per task)
- Wave 2 (Plan 06-02) can `import { test, expect } from "../fixtures/test-fixtures"`, destructure `propagationPair` / `propagationTimeline` / `api`, call `api.getIssue(id)` / `api.clearIssueDate(id, "target_date")` / `propagationTimeline.dragBlockBy(id, 4, issue)` / `propagationTimeline.getBlockBox(id)` without further infrastructure changes
- Plan 06-01 SUMMARY records: actual `clearIssueDate` HTTP status (200 vs 204), actual `dragBlockBy` mousedown approach (native vs dispatchEvent), actual OxLint warning count diff, full E2E suite runtime
  </success_criteria>

<output>
After completion, create `.planning/phases/06-end-to-end-coverage-polish/06-01-SUMMARY.md`
</output>
