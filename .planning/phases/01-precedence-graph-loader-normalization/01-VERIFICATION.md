---
phase: 01-precedence-graph-loader-normalization
verified: 2026-05-03T15:55:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 1: Precedence Graph Loader & Normalization Verification Report

**Phase Goal:** A pure-data graph builder that reads `IssueRelation` rows for a project and returns a `predecessor → successor` adjacency, normalizing `blocking` and `blocked_by` into a single direction and rejecting cycles, with zero coupling to Django views, HTTP, or serializers. Done = `apps/api/plane/app/services/timeline_propagation/graph.py` exposes a small interface that takes a project id + ORM accessor and returns a normalized adjacency object with cycle detection, fully covered by `pytest -m unit` tests with no DRF dependencies.
**Verified:** 2026-05-03T15:55:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth                                                                                                                                                                                                                        | Status   | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Given a fixture project with `blocking` / `blocked_by` / `relates_to` / `duplicate` relations, the loader returns adjacency containing **only** precedence edges normalized predecessor→successor.                           | VERIFIED | `graph.py:109` filters `relation_type != "blocked_by"` and `continue`s; `relates_to`, `duplicate`, `start_before`, `finish_before`, `implemented_by` are dropped. Pinned by `TestLoadPrecedenceGraphFilters::test_relates_to_is_dropped` (PASSED) and `::test_duplicate_is_dropped` (PASSED). Direction normalization: `graph.py:168-169` emits `Edge(predecessor_id=row.related_issue_id, successor_id=row.issue_id)`. Pinned by `TestLoadPrecedenceGraphDirection::test_predecessor_is_related_issue_successor_is_issue` (PASSED) which also asserts the reverse direction is empty. `TestLoadPrecedenceGraphFilters::test_blocking_via_get_actual_relation_normalizes_to_one_edge` (PASSED) confirms exactly one edge per `blocking`/`blocked_by` pair (no double-counting from a synthesized mirror).                                                                    |
| 2   | Given a graph that contains a cycle on the precedence subgraph, the loader surfaces a typed cycle result that the algorithm layer can convert into `DEPENDENCY_CYCLE` (no exceptions thrown across the module boundary).     | VERIFIED | `_detect_cycle` in `graph.py:175-228` is iterative three-color DFS using an explicit `list` stack of `(node, iter)` pairs — no recursion. On first back-edge, returns the closed cycle path tuple `(a, …, b, a)`; otherwise returns `None`. `LoadResult.cycle: tuple[UUID, ...]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | None` (`types.py:92`) carries this typed result without raising. Pinned by `TestLoadPrecedenceGraphCycle::test_three_node_cycle_is_detected`(PASSED — asserts`result.cycle[0] == result.cycle[-1]`and`set(result.cycle) == {a.id, b.id, c.id}`) and `::test_self_edge_is_one_node_cycle`(PASSED —`result.cycle == (a.id, a.id)`). `grep sys\.setrecursionlimit graph.py` returns no matches. |
| 3   | Given a relation that points to an issue in a different project, the loader marks the edge as cross-project so the algorithm layer can fail with `PROJECT_BOUNDARY_EXCEEDED` without ever loading the foreign issue's dates. | VERIFIED | `_make_edge` in `graph.py:136-172` reads BOTH endpoints' `project_id` (preferring annotations `issue_project_id` / `related_project_id`, falling back to `row.issue.project_id` / `row.related_issue.project_id`); flags the edge `cross_project=True` if EITHER endpoint differs from loader `project_id`. `graph.py:114-116` routes cross-project edges into `cross_project_edges` and `continue`s without entering `successors`/`predecessors`/`nodes`. Pinned by `TestLoadPrecedenceGraphCrossProject::test_cross_project_successor_marked` (PASSED — asserts `result.adjacency.successors_of(local.id) == frozenset()`, `result.adjacency.nodes == frozenset()`, `len(cross_project_edges) == 1`, `edge.cross_project is True`). The loader reads only `project_id` (no name/description/start_date/target_date dereferenced) — verified by inspection of `_make_edge`. |
| 4   | The module has no `from rest_framework`, no `from django.http`, and no view/serializer imports — verifiable by lint/grep — proving it is independently testable.                                                             | VERIFIED | `grep -RnE "^from rest_framework                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | ^from django\.http                                                                                                                                                                                                                                                                                                                                                                           | ^from plane\.app\.views | ^from plane\.app\.serializers" apps/api/plane/app/services/timeline_propagation/`returns no matches. The lint-grep test`test_no_drf_or_http_imports_in_module`(PASSED — module-level,`@pytest.mark.unit`, no DB) walks `pathlib.Path.rglob("\*.py")`over the package and asserts none of`rest_framework`, `django.http`, `plane.app.views`, `plane.app.serializers` substrings are present. This invariant remains pinned for future Phase 2 modules added to the same package. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact                                                                | Expected                                                                                                                               | Status   | Details                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/api/plane/app/services/__init__.py`                               | Empty services umbrella package marker (4-line license header, no imports)                                                             | VERIFIED | 145 bytes, license-only marker.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `apps/api/plane/app/services/timeline_propagation/__init__.py`          | Re-export barrel for 5 public symbols + PROP-18 module docstring                                                                       | VERIFIED | 661 bytes. Re-exports `Adjacency`, `Edge`, `LoadResult`, `WorkItemNode` (from `.types`) and `load_precedence_graph` (from `.graph`); `__all__` enumerates the 5 symbols. PROP-18 declared at module surface (`__init__.py:10`).                                                                                                                                                                                                                   |
| `apps/api/plane/app/services/timeline_propagation/types.py`             | 4 frozen+slots dataclasses with locked field shape; `successors_of` / `predecessors_of` no-KeyError                                    | VERIFIED | 3419 bytes. Four `@dataclass(frozen=True, slots=True)` classes (`WorkItemNode`, `Edge`, `Adjacency`, `LoadResult`); `Adjacency.successors_of`/`predecessors_of` return `frozenset()` for unknown ids. PEP-585/PEP-604 generics; `Mapping` from `collections.abc`; no `from __future__ import annotations`. PROP-18 declared in module docstring (`types.py:20`).                                                                                  |
| `apps/api/plane/app/services/timeline_propagation/graph.py`             | Loader + iterative three-color DFS + Pitfall 1 directionality comment + cross-project both-endpoints rule                              | VERIFIED | 9211 bytes. `load_precedence_graph(relations, project_id)` filters `blocked_by`-only with defensive soft-delete skip; `_make_edge` does both-endpoints cross-project classification; `_detect_cycle` iterative DFS with self-edge guard. Pitfall 1 comment present at `graph.py:118-119` ("predecessor=Y, successor=X"). PROP-18 declared in module docstring (`graph.py:48`). No `sys.setrecursionlimit`; no DRF/HTTP/views/serializers imports. |
| `apps/api/plane/tests/unit/services/__init__.py`                        | Empty test sub-package marker                                                                                                          | VERIFIED | 145 bytes, license-only marker.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `apps/api/plane/tests/unit/services/timeline_propagation/__init__.py`   | Empty test sub-package marker                                                                                                          | VERIFIED | 145 bytes, license-only marker.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `apps/api/plane/tests/unit/services/timeline_propagation/test_graph.py` | 10 (effectively 11) unit tests covering filter / direction / cycle / cross-project / empty / adjacency shape / convenience / lint-grep | VERIFIED | 16565 bytes. 11 tests collected, 11 PASSED in 1.17s. Coverage map docstring present at top. `Adjacency` imported from `.types` for the convenience-method test. No DRF/HTTP/views/serializers imports in the test file itself.                                                                                                                                                                                                                    |

### Key Link Verification

| From            | To                             | Via                                                                         | Status | Details                                                                                                                            |
| --------------- | ------------------------------ | --------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------- |
| `__init__.py`   | `types.py`                     | `from .types import Adjacency, Edge, LoadResult, WorkItemNode`              | WIRED  | Verified at `__init__.py:14`.                                                                                                      |
| `__init__.py`   | `graph.py`                     | `from .graph import load_precedence_graph`                                  | WIRED  | Verified at `__init__.py:13`. Cascade-imports succeed (Plan 02's `graph.py` resolves the Plan 01 RED forward reference).           |
| `graph.py`      | `types.py`                     | `from .types import Adjacency, Edge, LoadResult`                            | WIRED  | Verified at `graph.py:57`.                                                                                                         |
| `test_graph.py` | `timeline_propagation` package | `from plane.app.services.timeline_propagation import load_precedence_graph` | WIRED  | Verified at `test_graph.py:30`. Test imports also `Adjacency` from `.types` at `test_graph.py:31` for the convenience-method test. |

### Data-Flow Trace (Level 4)

| Artifact                          | Data Variable                                                       | Source                                                                                                               | Produces Real Data                                                                                                                                                                                                                                                                                                           | Status  |
| --------------------------------- | ------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| `graph.py::load_precedence_graph` | `Adjacency.successors`/`predecessors`/`nodes`/`cross_project_edges` | `relations: Iterable[RelationLike]` (caller passes `IssueRelation.objects.filter(...)` queryset)                     | Yes — pytest tests build real `IssueRelation` rows via `IssueRelation.objects.create(...)`, queryset is iterated, edges constructed and rendered into `Adjacency` mappings. The 11 passing tests assert on real data flowing through (e.g., `test_chain_split_merge_adjacency_contents` asserts on 4 edges across 5 issues). | FLOWING |
| `graph.py::_detect_cycle`         | `LoadResult.cycle`                                                  | `Adjacency.successors` walked via iterative three-color DFS                                                          | Yes — `test_three_node_cycle_is_detected` asserts the returned tuple contains the actual created issue ids, not a stub.                                                                                                                                                                                                      | FLOWING |
| `graph.py::_make_edge`            | `Edge.cross_project`                                                | `row.issue.project_id` and `row.related_issue.project_id` (or annotated equivalents) compared to loader `project_id` | Yes — `test_cross_project_successor_marked` builds two real projects and verifies the cross-project flag on the resulting edge.                                                                                                                                                                                              | FLOWING |

### Behavioral Spot-Checks

| Behavior                                          | Command                                                                                                                                                                        | Result                                                           | Status |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------- | ------ |
| Phase 1 unit suite passes 11/11                   | `docker exec plane-api-1 sh -c "cd /code && DJANGO_SETTINGS_MODULE=plane.settings.test pytest plane/tests/unit/services/timeline_propagation/test_graph.py -v"`                | `11 passed, 3 warnings in 1.17s`                                 | PASS   |
| Test inventory has 11 collectable items           | `docker exec plane-api-1 sh -c "cd /code && DJANGO_SETTINGS_MODULE=plane.settings.test pytest plane/tests/unit/services/timeline_propagation/test_graph.py --collect-only -q"` | `11 tests collected in 0.03s`                                    | PASS   |
| Forbidden-import grep is empty                    | `grep -RnE "^from rest_framework\|^from django\.http\|^from plane\.app\.views\|^from plane\.app\.serializers" apps/api/plane/app/services/timeline_propagation/`               | No matches                                                       | PASS   |
| No `sys.setrecursionlimit` anywhere in `graph.py` | `grep -n "sys\.setrecursionlimit" apps/api/plane/app/services/timeline_propagation/graph.py`                                                                                   | No matches                                                       | PASS   |
| Directionality rule present verbatim              | `grep -n "predecessor=Y, successor=X\|predecessor_id=row.related_issue_id\|successor_id=row.issue_id" graph.py`                                                                | Found at lines 119, 168, 169                                     | PASS   |
| PROP-18 declared on module surface                | `grep -n "PROP-18" apps/api/plane/app/services/timeline_propagation/`                                                                                                          | Found in 3 files: `types.py:20`, `graph.py:48`, `__init__.py:10` | PASS   |

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                                                                     | Status    | Evidence                                                                                                                                                                                                                                                                                                                                                                            |
| ----------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PROP-01     | 01-02-PLAN  | Server builds same-project precedence graph from `IssueRelation`, normalizing `blocking`/`blocked_by` to `predecessor→successor` (US-34, US-16) | SATISFIED | `_make_edge` direction translation (`graph.py:168-169`); `TestLoadPrecedenceGraphDirection::test_predecessor_is_related_issue_successor_is_issue` and `TestLoadPrecedenceGraphFilters::test_blocking_via_get_actual_relation_normalizes_to_one_edge` both PASSED. REQUIREMENTS.md marks `[x]` Done with traceability (line 19).                                                     |
| PROP-02     | 01-02-PLAN  | `relates_to` / `duplicate` excluded from graph (US-17, US-18)                                                                                   | SATISFIED | `graph.py:109` `if row.relation_type != "blocked_by": continue`; `TestLoadPrecedenceGraphFilters::test_relates_to_is_dropped` and `::test_duplicate_is_dropped` both PASSED. REQUIREMENTS.md marks `[x]` Done (line 20).                                                                                                                                                            |
| PROP-15     | 01-02-PLAN  | Cycle on precedence graph stops propagation, returns `DEPENDENCY_CYCLE` (US-28)                                                                 | SATISFIED | `_detect_cycle` iterative three-color DFS (`graph.py:175-228`); `LoadResult.cycle` carries closed cycle path; `TestLoadPrecedenceGraphCycle::test_three_node_cycle_is_detected` and `::test_self_edge_is_one_node_cycle` both PASSED. Phase 2 will translate this typed result into `DEPENDENCY_CYCLE` failure. REQUIREMENTS.md marks `[x]` Done (line 33).                         |
| PROP-16     | 01-02-PLAN  | Out-of-project paths stop propagation, return `PROJECT_BOUNDARY_EXCEEDED` (US-20)                                                               | SATISFIED | `_make_edge` both-endpoints cross-project classification (`graph.py:157-172`); cross-project edges routed to `cross_project_edges` and excluded from same-project adjacency; `TestLoadPrecedenceGraphCrossProject::test_cross_project_successor_marked` PASSED. Phase 2/3 will translate reachability into `PROJECT_BOUNDARY_EXCEEDED`. REQUIREMENTS.md marks `[x]` Done (line 34). |
| PROP-18     | 01-01-PLAN  | Move-only scope; resize out of scope (Implementation Decision: move only)                                                                       | SATISFIED | Module-surface declaration in 3 files (`types.py:20`, `graph.py:48`, `__init__.py:10`). REQUIREMENTS.md marks `[x]` Done (line 36).                                                                                                                                                                                                                                                 |
| TEST-11     | 01-02-PLAN  | Backend service unit test: cycle detection → `DEPENDENCY_CYCLE`                                                                                 | SATISFIED | `TestLoadPrecedenceGraphCycle::test_three_node_cycle_is_detected` covers transitive 3-node cycle; `::test_self_edge_is_one_node_cycle` covers degenerate self-loop. Both PASSED. REQUIREMENTS.md marks `[x]` Done (line 92).                                                                                                                                                        |

All 6 phase-assigned requirement IDs are accounted for in plan frontmatter (`01-01-PLAN.md` claims PROP-18; `01-02-PLAN.md` claims PROP-01, PROP-02, PROP-15, PROP-16, TEST-11). No orphaned requirements detected.

### Anti-Patterns Found

| File   | Line | Pattern                                                                                                                                                                                                                                                              | Severity | Impact     |
| ------ | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- |
| (none) | —    | No `TODO` / `FIXME` / `placeholder` / `coming soon` / `not yet implemented` strings detected anywhere under `apps/api/plane/app/services/timeline_propagation/`.                                                                                                     | —        | No issues. |
| (none) | —    | No empty-implementation patterns (`return None` as stub, `pass` standalone, hardcoded empty literals as final return). The `frozenset()` defaults in `successors_of`/`predecessors_of` are part of the documented D-06 contract (no-KeyError invariant), not a stub. | —        | No issues. |
| (none) | —    | No console-only / log-only handler patterns.                                                                                                                                                                                                                         | —        | No issues. |

### Human Verification Required

No human verification items identified. Phase 1 is a pure-Python module with no UI, no real-time behavior, no external service integration. All correctness is exercisable from `pytest -m unit`, and all 11 tests passed in the docker container. The deferred-items.md file documents 5 pre-existing unit-suite failures in unrelated files (`bg_tasks/test_copy_s3_objects.py`, `bg_tasks/test_work_item_link_task.py`, `utils/test_url.py`); these were verified by Plan 02 to fail on the parent commit and on the Plan 01-01 tip — not introduced by this phase.

### Gaps Summary

No gaps. All 4 ROADMAP success criteria, all 6 phase requirements, all 7 expected artifacts, all 4 key links, and all 6 behavioral spot-checks pass with direct codebase evidence:

- The loader is a pure free function over `Iterable[RelationLike]` (no DRF/HTTP/views/serializers imports anywhere in the package, verified by both grep and an in-suite lint-grep test).
- Direction normalization is encoded verbatim per Pitfall 1 (`predecessor_id=row.related_issue_id`, `successor_id=row.issue_id`) with the rationale comment present at the construction site.
- Cycle detection is iterative three-color DFS (no recursion, no `sys.setrecursionlimit`) returning a typed `tuple[UUID, ...] | None` result; the loader never throws across the module boundary.
- Cross-project classification reads BOTH endpoints' `project_id` (correctly enforcing PROP-16 "paths reaching outside the project fail propagation" semantics), prefers precomputed annotations, and never dereferences any other foreign-issue field.
- Public surface (`Adjacency`, `Edge`, `LoadResult`, `WorkItemNode`, `load_precedence_graph`) is locked via `__init__.py` re-exports; all four dataclasses are `@dataclass(frozen=True, slots=True)` with `Adjacency.successors_of`/`predecessors_of` returning empty frozenset for unknown ids.
- 11/11 tests pass green in the `plane-api-1` container; all 6 phase requirements are marked `[x]` Done in `REQUIREMENTS.md` with full traceability lines.

Phase 2 is unblocked; the `LoadResult` / `Adjacency` / `Edge` / `WorkItemNode` shapes are now contractually frozen for downstream consumers.

---

_Verified: 2026-05-03T15:55:00Z_
_Verifier: Claude (gsd-verifier)_
