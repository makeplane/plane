---
phase: 04-workspace-template-management
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - apps/web/core/components/project-templates/list/template-row.tsx
  - apps/web/core/components/project-templates/list/root.tsx
  - apps/web/core/components/project-templates/deactivate-modal.tsx
  - packages/i18n/src/locales/en/workspace-settings.json
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "A custom active row exposes Edit + Duplicate primary actions and Deactivate in the overflow (⋮) menu"
    - "Deactivating a custom template confirms via AlertModalCore, calls DELETE, toasts success, and the row leaves the active list"
    - "Duplicate (on custom and built-in rows) creates an editable custom copy and refreshes the list"
    - "Built-in rows expose only Duplicate + View (read-only), never Edit or Deactivate"
    - "Turning on 'Show deactivated' re-fetches with include_inactive=true and reveals deactivated custom rows with a Reactivate action that flips them back to active"
  artifacts:
    - apps/web/core/components/project-templates/list/template-row.tsx
    - apps/web/core/components/project-templates/deactivate-modal.tsx
    - apps/web/core/components/project-templates/list/root.tsx
  key_links:
    - "Show-deactivated toggle -> getProjectTemplates(slug, includeInactive=true) SWR key variant -> deactivated custom rows"
    - "row actions -> projectService.deactivate/reactivate/duplicateProjectTemplate -> mutate(WORKSPACE_PROJECT_TEMPLATES)"
    - "deactivate -> AlertModalCore variant=danger -> DELETE (soft) -> toast"
    - "Edit action -> navigate /settings/templates/:id/edit ; View (built-in) -> read-only editor route"
---

<objective>
Complete the lifecycle slice: wire the custom-template row actions (Edit, Duplicate, Deactivate) and built-in row actions (Duplicate, View) per D-07/D-08, add the deactivate confirmation modal, and implement the "Show deactivated" toggle (D-06) that re-fetches with `include_inactive=true` (the Plan 01 backend slice) and surfaces a Reactivate action (D-15) on deactivated custom rows. After this plan the full custom-template management lifecycle works end-to-end. The plan closes with the end-of-phase human verification of UI-05.

Purpose: This connects the list (Plan 02), the editor (Plans 03/04), the service methods (Plan 03), and the backend include_inactive/reactivate slice (Plan 01) into the complete admin management experience UI-05 requires.
Output: row action wiring, deactivate/reactivate confirmation, the Show-deactivated filter, and the phase human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-workspace-template-management/04-CONTEXT.md
@.planning/phases/04-workspace-template-management/04-RESEARCH.md
@.planning/phases/04-workspace-template-management/04-PATTERNS.md
@.planning/phases/04-workspace-template-management/04-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan produces:

- Fully wired `ProjectTemplateRow` actions (Edit/Duplicate/Deactivate/Reactivate/View) matching the D-07/D-08 row-action matrix.
- `ProjectTemplateDeactivateModal` (AlertModalCore wrapper) handling both deactivate and reactivate confirmation.
- Show-deactivated toggle in `list/root.tsx` driving an `include_inactive=true` SWR fetch (distinct SWR key so active-only cache stays intact).
- i18n keys for the row actions, toggle, confirmation, and success toasts.
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire row actions, deactivate/reactivate modal, and duplicate/edit/view navigation</name>
  <files>apps/web/core/components/project-templates/deactivate-modal.tsx, apps/web/core/components/project-templates/list/template-row.tsx, packages/i18n/src/locales/en/workspace-settings.json</files>
  <read_first>
    - apps/web/core/components/project-templates/list/template-row.tsx (Plan 02 — has the row shell + actions slot to fill)
    - apps/web/core/components/project-states/state-delete-modal.tsx (AlertModalCore wrapper: local isSubmitting, .then(close).catch(toast).finally — the deactivate-modal analog ~24-80)
    - packages/ui/src/modals/alert-modal.tsx (AlertModalCore props: variant danger, primaryButtonText {loading,default} ~19-73)
    - packages/ui/src/dropdowns/custom-menu.tsx (CustomMenu ellipsis + MenuItem ~540)
    - apps/web/core/services/project/project.service.ts (Plan 03 — deactivate/reactivate/duplicateProjectTemplate)
    - .planning/phases/04-workspace-template-management/04-PATTERNS.md (template-row 189-222; deactivate-modal 226-248)
    - .planning/phases/04-workspace-template-management/04-UI-SPEC.md (Copywriting row actions + confirmation + success toasts; Color: Deactivate is the only destructive action)
  </read_first>
  <action>
    `deactivate-modal.tsx` (`ProjectTemplateDeactivateModal`): AlertModalCore wrapper mirroring state-delete-modal — local `isSubmitting`, `handleSubmit` calls `projectService.deactivateProjectTemplate(slug, id)` then `mutate(WORKSPACE_PROJECT_TEMPLATES(slug))`, `setToast` success ("Template deactivated"), `.catch` error toast, `.finally` reset. Use `variant="danger"` and the exact confirmation copy from UI-SPEC ("Deactivate template?" / body / Confirm "Deactivate" / Cancel). Support a reactivate mode reusing the same shell (primary/neutral variant, "Template reactivated" toast) calling `reactivateProjectTemplate`, OR implement reactivate as a direct row action with its own toast (Claude's Discretion — reactivate is not destructive so an inline action without a modal is acceptable).
    `template-row.tsx`: fill the actions slot per the D-07/D-08 matrix using the row's `is_system`/`is_active` flags:
    - custom + active: Edit (navigate `/${slug}/settings/templates/${id}/edit`) + Duplicate (primary actions) + `CustomMenu` (⋮) containing Deactivate (opens the modal).
    - custom + deactivated (only rendered when Show-deactivated is on): Reactivate (primary) + `CustomMenu` (⋮) with Edit/Duplicate; render the row muted (`text-tertiary`, reduced opacity) per UI-SPEC.
    - built-in: Duplicate + View only (navigate to the edit route in read-only mode, per Plan 03's is_system read-only branch). No Edit, no Deactivate (D-08).
    Duplicate calls `projectService.duplicateProjectTemplate(slug, id)` then `mutate` + success toast ("Template duplicated"); Claude's Discretion whether to prompt for a name inline first or open the new copy pre-filled (backend accepts optional name).
    All actions go through `projectService`; use `CustomMenu` for the overflow, semantic tokens only, accent reserved for the primary CTA (Deactivate uses the danger tokens; Edit/Duplicate are neutral, not accent).
    Add the required i18n keys (row action labels, confirmation, success toasts) under `workspace_settings.settings.project_templates.*`.
  </action>
  <verify>
    <automated>pnpm --filter=web check:types && pnpm --filter=web check:lint</automated>
  </verify>
  <acceptance_criteria>
    - `check:types` and `check:lint` pass; no new warnings.
    - `template-row.tsx` renders Edit+Duplicate+(⋮ Deactivate) for custom active rows, Reactivate+(⋮) for custom deactivated rows, and Duplicate+View only for built-in rows (grep the is_system / is_active branching).
    - Built-in rows render NO Edit or Deactivate control (grep: no edit/deactivate affordance in the is_system branch).
    - `deactivate-modal.tsx` uses `AlertModalCore` with `variant="danger"`, calls `deactivateProjectTemplate`, and `mutate(WORKSPACE_PROJECT_TEMPLATES(...))` on success (grep).
    - Duplicate and reactivate actions call `duplicateProjectTemplate` / `reactivateProjectTemplate` and revalidate (grep).
    - No inline axios/fetch.
  </acceptance_criteria>
  <done>Row actions match the D-07/D-08 matrix, deactivate confirms and soft-deletes, duplicate/reactivate work and refresh the list, built-ins stay read-only; type-check + lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Show-deactivated filter fetching include_inactive rows</name>
  <files>apps/web/core/components/project-templates/list/root.tsx</files>
  <read_first>
    - apps/web/core/components/project-templates/list/root.tsx (Plan 02 — the two-section SWR list)
    - apps/web/core/services/project/project.service.ts (Plan 03 — `getProjectTemplates(slug, includeInactive?)`)
    - apps/api/plane/app/views/workspace/project_template.py (Plan 01 — include_inactive returns deactivated CUSTOM rows only; built-ins stay active)
    - packages/constants/src/fetch-keys.ts (`WORKSPACE_PROJECT_TEMPLATES`)
    - .planning/phases/04-workspace-template-management/04-PATTERNS.md (SWR list fetch section 428-441; list/root 159-186)
    - .planning/phases/04-workspace-template-management/04-UI-SPEC.md (Show-deactivated toggle placement + empty copy "No deactivated templates.")
  </read_first>
  <action>
    In `list/root.tsx` add a "Show deactivated" toggle in the Custom section header (right-aligned control), off by default (D-06). Hold its state locally. When ON, fetch with `include_inactive=true`: call `projectService.getProjectTemplates(workspaceSlug, true)` under a DISTINCT SWR key variant (e.g. suffix the `WORKSPACE_PROJECT_TEMPLATES(slug)` key with `_INCLUDE_INACTIVE`) so the active-only cache used by the Phase 3 create-modal selector is never overwritten (D-14). When OFF, use the existing active-only key/fetch from Plan 02.
    Filter the fetched rows into the two sections; when Show-deactivated is on, render deactivated custom rows (is_active=false, is_system=false) in the Custom section using the muted styling + Reactivate action from Task 1. When the toggle is on and there are zero deactivated rows, show the "No deactivated templates." copy. Built-ins remain active-only and read-only regardless of the toggle.
    After any mutation (deactivate/reactivate/duplicate/create/update) revalidate BOTH the active-only key and, when relevant, the include-inactive key so the visible list is consistent.
    The toggle's engaged state uses the accent token per UI-SPEC (accent reserved list). Use existing primitives + semantic tokens only.
  </action>
  <verify>
    <automated>pnpm --filter=web check:types && pnpm --filter=web check:lint</automated>
    <human-check>
      End-of-phase human verification of UI-05. Run the web app as a workspace ADMIN:
      1. Workspace Settings shows "Project Templates" in the Administration group; it opens /settings/templates. A non-admin gets Not Authorized at the same URL.
      2. System templates (Software Project, Marketing Campaign, Operations Project) render read-only (Duplicate + View only; no Edit/Deactivate).
      3. New template: add name, 2+ states (exactly one Default, colors, drag reorder), 1+ label, 1 module, 1 cycle (start_offset_days <= target_offset_days), 1 starter issue referencing state/label/module/cycle by name. Save -> success toast, appears under Custom templates.
      4. Edit it, rename a referenced state, save -> no dangling-reference error (stable keys). Try zero default states or start>target -> client blocks / inline error.
      5. Duplicate a built-in -> editable custom copy appears. Deactivate a custom template (confirm modal) -> leaves active list. Toggle "Show deactivated" -> appears muted; Reactivate -> returns to active list.
      6. The create-Project modal selector (Phase 3) still lists active templates only (unaffected by include_inactive).
    </human-check>
  </verify>
  <acceptance_criteria>
    - `check:types` and `check:lint` pass; no new warnings.
    - `list/root.tsx` renders a "Show deactivated" toggle defaulting OFF and, when ON, calls `getProjectTemplates(workspaceSlug, true)` under a distinct SWR key that does not clobber the active-only key (grep the key variant + the `true` arg).
    - Deactivated custom rows appear only when the toggle is on, rendered muted with a Reactivate action; built-ins never appear as deactivated (grep the filter).
    - Toggle-on with no deactivated rows shows the "No deactivated templates." copy.
    - Human verification of the full admin lifecycle (steps 1-6 above) passes.
  </acceptance_criteria>
  <done>Admins can reveal deactivated custom templates via the toggle (backed by include_inactive), reactivate them, and the create-modal's active-only cache is never disturbed; type-check + lint pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                    |
| ----------------------------------------------- | ------------------------------------------------------------------------------ |
| row action client -> mutation API               | Deactivate/reactivate/duplicate requests cross to admin-only backend endpoints |
| Show-deactivated fetch <- include_inactive list | Deactivated custom rows returned to the management UI only                     |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                       |
| --------- | ---------------------- | ----------------------------------------------- | -------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-15   | Elevation of Privilege | deactivate/reactivate/duplicate row actions     | high     | mitigate    | Actions only reachable inside the admin-gated page (Plan 02 guard); backend endpoints are ADMIN-only and reactivate rejects built-in/foreign (Plan 01, verified by tests). UI gate is UX; backend authoritative (V4). |
| T-04-16   | Information Disclosure | Show-deactivated include_inactive fetch         | high     | mitigate    | Backend include_inactive returns only same-workspace custom rows, never inactive built-ins or foreign rows (Plan 01 tests); distinct SWR key avoids leaking inactive rows into the create-modal cache (D-14).         |
| T-04-17   | Tampering              | reactivate/duplicate of built-in or foreign row | high     | mitigate    | Built-in rows expose no reactivate/deactivate; backend rejects built-in reactivate (400) and foreign (404) regardless of UI (Plan 01).                                                                                |
| T-04-18   | Cross-site scripting   | rendering names in rows/menus                   | medium   | mitigate    | React auto-escapes; no `dangerouslySetInnerHTML` for template-supplied strings (V5).                                                                                                                                  |

</threat_model>

<verification>
- `pnpm --filter=web check:types` and `pnpm --filter=web check:lint` pass.
- Backend behavior underpinning D-06/D-07 verified by Plan 01's contract tests (include_inactive listing, reactivate, permission + built-in/foreign rejection).
- End-of-phase human-verify checkpoint confirms the full admin lifecycle and that the Phase 3 selector remains active-only.
</verification>

<success_criteria>

- The complete custom-template management lifecycle (create/edit/duplicate/deactivate/reactivate + show-deactivated) works end-to-end for admins.
- Built-ins stay read-only; the Phase 3 create-modal selector is unaffected.
- UI-05 is satisfied and human-verified; all type-checks and lint pass.
  </success_criteria>

<output>
Create `.planning/phases/04-workspace-template-management/04-05-SUMMARY.md` when done.
</output>
