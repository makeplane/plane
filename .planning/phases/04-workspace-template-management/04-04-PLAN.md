---
phase: 04-workspace-template-management
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - apps/web/core/components/project-templates/editor/modules-section.tsx
  - apps/web/core/components/project-templates/editor/cycles-section.tsx
  - apps/web/core/components/project-templates/editor/starter-issues-section.tsx
  - apps/web/core/components/project-templates/editor/root.tsx
  - apps/web/core/components/project-templates/utils.ts
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "An admin can add/edit/remove Modules rows (name, description, status enum) in the editor"
    - "An admin can add/edit/remove Cycles rows (name, description, offset/duration) with start_offset_days <= target_offset_days enforced before save"
    - "An admin can add Starter issues that reference states/labels/modules/cycles picked BY NAME from items defined above, resolved to stable keys on save"
    - "Saving a template with all five sections produces one atomic payload the backend accepts (no dangling references, valid enums, valid offsets)"
  artifacts:
    - apps/web/core/components/project-templates/editor/modules-section.tsx
    - apps/web/core/components/project-templates/editor/cycles-section.tsx
    - apps/web/core/components/project-templates/editor/starter-issues-section.tsx
  key_links:
    - "modules/cycles/starter_issues useFieldArray in editor/root.tsx -> section components"
    - "starter-issue reference dropdowns fed from in-editor states/labels/modules/cycles items -> resolved to stable keys in assemblePayload"
    - "cycles client guard start_offset_days <= target_offset_days -> blocks Save"
---

<objective>
Complete the structured editor by adding the remaining three sections — Modules, Cycles, and Starter issues — so the atomic save covers all five payload sections (D-10/D-11). Starter-issue references are name-based dropdowns populated from the items defined in the sections above and resolved to stable keys on save (D-13), preventing the dangling-reference 400s the backend would otherwise reject. After this plan the editor authors a complete template covering states, labels, modules, cycles, and starter issues.

Purpose: This finishes the authoring surface for UI-05 and closes the cross-section reference-resolution logic that is the trickiest correctness concern (Pitfalls 2 and 4).
Output: Modules, Cycles, and Starter-issues section components; the editor root wired to their field arrays; and `assemblePayload` extended to resolve starter-issue references.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-workspace-template-management/04-CONTEXT.md
@.planning/phases/04-workspace-template-management/04-RESEARCH.md
@.planning/phases/04-workspace-template-management/04-PATTERNS.md
@.planning/phases/04-workspace-template-management/04-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan produces:

- Components `ModulesSection`, `CyclesSection`, `StarterIssuesSection`.
- Editor root field arrays for `modules`, `cycles`, `starter_issues` and their section rendering.
- Extended `assemblePayload` in `utils.ts` that resolves starter-issue name-selections to the stable `state_key`/`label_key`/`module_key`/`cycle_key` of the in-editor items.
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build Modules and Cycles sections with enum selects and offset validation</name>
  <files>apps/web/core/components/project-templates/editor/modules-section.tsx, apps/web/core/components/project-templates/editor/cycles-section.tsx, apps/web/core/components/project-templates/editor/root.tsx</files>
  <read_first>
    - apps/web/core/components/project-templates/editor/root.tsx (from Plan 03 — form shape already declares modules/cycles/starter_issues empty arrays; wire useFieldArray here)
    - apps/web/core/components/project-templates/editor/states-section.tsx (Plan 03 — mirror the inline add/edit/remove row idiom and hidden-key generation)
    - apps/web/core/components/project-templates/utils.ts (Plan 03 — slugifyKey/uniqueKey/assemblePayload)
    - apps/web/core/components/project-states/create-update/form.tsx (add/edit row form idiom — name Input + submit/cancel; drive via RHF field array, not local useState)
    - packages/ui/src/dropdowns/custom-select.tsx (CustomSelect props for enum dropdowns)
    - packages/types/src/project/project_templates.ts (`TProjectTemplateModule` status enum; `TProjectTemplateCycle` offset/duration fields)
    - apps/api/plane/app/serializers/project_template.py (offset rule start<=target ~363-376; #RRGGBB rule; enum validation — for parity)
    - .planning/phases/04-workspace-template-management/04-PATTERNS.md (modules/cycles/starter section, lines 315-323)
    - .planning/phases/04-workspace-template-management/04-UI-SPEC.md (Screen 2 sections; Copywriting per-section add/empty; no drag for these sections)
  </read_first>
  <action>
    Add `useFieldArray` for `modules` and `cycles` in `editor/root.tsx` and render the two new sections (no drag handles — order is irrelevant for modules/cycles per D-11).
    `modules-section.tsx` (`ModulesSection`): inline add/edit/remove rows driven by the `modules` field array. Each row: name `Input` (required, maxLength 255), description `TextArea`/`Input`, and status via `CustomSelect` constrained to the enum backlog|planned|in-progress|paused|completed|cancelled. Generate a stable hidden `module_key` at add-time via `slugifyKey`+`uniqueKey` (D-12). Empty hint + "Add module" control per UI-SPEC copy.
    `cycles-section.tsx` (`CyclesSection`): inline add/edit/remove rows driven by the `cycles` field array. Each row: name `Input`, description, and optional numeric `start_offset_days` / `target_offset_days` / `duration_days`. Enforce `start_offset_days <= target_offset_days` client-side before enabling Save and surface inline when violated (Pitfall 4). Generate a stable hidden `cycle_key` at add-time (D-12). Empty hint + "Add cycle" control.
    Both sections use only existing primitives and semantic tokens; no raw hex, no new UI primitives.
  </action>
  <verify>
    <automated>pnpm --filter=web check:types && pnpm --filter=web check:lint</automated>
  </verify>
  <acceptance_criteria>
    - `check:types` and `check:lint` pass; no new lint warnings.
    - `modules-section.tsx` constrains status to the six-value enum via `CustomSelect` (grep the enum values).
    - `cycles-section.tsx` blocks Save / surfaces inline when `start_offset_days > target_offset_days` (grep the offset comparison).
    - Modules and cycles generate hidden `module_key`/`cycle_key` at add-time; no raw key input is rendered (grep: no visible Input bound to `module_key`/`cycle_key`).
    - `editor/root.tsx` declares `useFieldArray` for `modules` and `cycles` (grep).
  </acceptance_criteria>
  <done>Modules and Cycles sections author rows with enum-constrained status and validated offsets, wired into the editor's atomic form.</done>
</task>

<task type="auto">
  <name>Task 2: Build Starter-issues section with name-based reference dropdowns resolved to keys on save</name>
  <files>apps/web/core/components/project-templates/editor/starter-issues-section.tsx, apps/web/core/components/project-templates/editor/root.tsx, apps/web/core/components/project-templates/utils.ts</files>
  <read_first>
    - apps/web/core/components/project-templates/editor/root.tsx (wire starter_issues useFieldArray; pass current states/labels/modules/cycles field values into the section for reference options)
    - apps/web/core/components/project-templates/utils.ts (extend assemblePayload here)
    - packages/ui/src/dropdowns/custom-select.tsx (CustomSelect for reference + priority dropdowns)
    - packages/types/src/project/project_templates.ts (`TProjectTemplateStarterIssue` — state_key/label_keys/module_key/cycle_key/priority)
    - apps/api/plane/app/serializers/project_template.py (`references unknown *_key` validation — the dangling-reference rejection this resolves)
    - .planning/phases/04-workspace-template-management/04-RESEARCH.md (Pitfall 2 key-drift; D-13 resolution)
    - .planning/phases/04-workspace-template-management/04-PATTERNS.md (starter-issues section, lines 315-321; No Analog note lines 466-471)
  </read_first>
  <action>
    Add `useFieldArray` for `starter_issues` in `editor/root.tsx` and render `StarterIssuesSection`.
    `starter-issues-section.tsx` (`StarterIssuesSection`): inline add/edit/remove rows. Each row: name `Input` (required); priority via `CustomSelect` (enum urgent|high|medium|low|none); and reference dropdowns for state (single), labels (multi), module (single), cycle (single) — each `CustomSelect` whose OPTIONS are the current in-editor items from the states/labels/modules/cycles field arrays, displayed BY NAME (D-13). Store the selection as a reference to the in-editor item (e.g. by its stable field key), NOT as a recomputed slug. Do not show raw keys (D-12). References are optional/nullable per the type.
    Extend `assemblePayload` in `utils.ts`: when building each `starter_issues` entry, resolve the selected in-editor item references to their STABLE stored keys (`state_key`, `label_keys[]`, `module_key`, `cycle_key`) — never recompute from names (Pitfall 2). If a referenced item was removed, drop the dangling reference (or block save with an inline message) so the payload never references an unknown key. Keep `schema_version: 1`.
    Handle the two edge behaviors: (a) renaming a referenced item keeps the reference valid because keys are stable; (b) deleting a referenced item clears the now-invalid reference.
  </action>
  <verify>
    <automated>pnpm --filter=web check:types && pnpm --filter=web check:lint</automated>
  </verify>
  <acceptance_criteria>
    - `check:types` and `check:lint` pass; no new warnings.
    - `starter-issues-section.tsx` feeds reference dropdown options from the in-editor states/labels/modules/cycles field values (grep the props/derivation), displays them by name, and constrains priority to the five-value enum.
    - `assemblePayload` resolves starter-issue references to stored stable keys and does not recompute keys from current names (grep: resolution reads a stored key field, not `slugifyKey(name)` at submit for references).
    - No raw `*_key` field is rendered as a visible input in the starter-issues rows.
    - Deleting a referenced item does not leave a dangling reference in the assembled payload (covered by the resolution/clear logic).
  </acceptance_criteria>
  <done>Starter issues reference states/labels/modules/cycles by name, resolve to stable keys on save, and never emit dangling references; the full five-section payload is assembled atomically.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                    |
| ---------------------------------- | -------------------------------------------------------------- |
| editor client -> create/update API | Full five-section user-authored payload crosses to the backend |

## STRIDE Threat Register

| Threat ID | Category             | Component                          | Severity | Disposition | Mitigation Plan                                                                                                                                       |
| --------- | -------------------- | ---------------------------------- | -------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-12   | Tampering            | dangling starter-issue references  | high     | mitigate    | References resolved from in-editor stable keys; removed items cleared before submit; backend re-validates `references unknown *_key` (Pitfall 2, V5). |
| T-04-13   | Tampering            | invalid enum / offset values       | medium   | mitigate    | Status/priority constrained to enum `CustomSelect`; cycles enforce start<=target client-side; backend authoritative (Pitfall 4, V5).                  |
| T-04-14   | Cross-site scripting | rendering module/cycle/issue names | medium   | mitigate    | React auto-escapes; no `dangerouslySetInnerHTML` for template-supplied strings (V5).                                                                  |

</threat_model>

<verification>
- `pnpm --filter=web check:types` and `pnpm --filter=web check:lint` pass.
- `assemblePayload` reference resolution uses stored stable keys (guards Pitfall 2); cycles guard start<=target (guards Pitfall 4).
- Functional end-to-end save of a full five-section template is confirmed at the phase human-verify (Plan 05 final task).
</verification>

<success_criteria>

- All five editor sections author valid rows; starter-issue references resolve to stable keys with no dangling references.
- Enum and offset client guards prevent the common backend 400s; type-check and lint pass.
  </success_criteria>

<output>
Create `.planning/phases/04-workspace-template-management/04-04-SUMMARY.md` when done.
</output>
