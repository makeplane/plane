---
phase: 04-workspace-template-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/settings.ts
  - packages/constants/src/settings/workspace.ts
  - apps/web/core/components/settings/workspace/sidebar/item-icon.tsx
  - apps/web/app/routes/core.ts
  - packages/i18n/src/locales/en/workspace-settings.json
  - apps/web/app/(all)/[workspaceSlug]/(settings)/settings/(workspace)/templates/page.tsx
  - apps/web/app/(all)/[workspaceSlug]/(settings)/settings/(workspace)/templates/header.tsx
  - apps/web/core/components/project-templates/list/root.tsx
  - apps/web/core/components/project-templates/list/template-row.tsx
  - apps/web/core/components/project-templates/list/loader.tsx
  - apps/web/core/components/project-templates/index.ts
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "A workspace admin sees a 'Project Templates' entry in the Administration group of workspace settings and can click it"
    - "The Project Templates page renders System templates and Custom templates as two labeled sections, populated end-to-end from the live list endpoint"
    - "A non-admin who navigates to /settings/templates sees NotAuthorizedView, not the list"
    - "Loading shows skeleton rows; a custom-empty workspace shows the EmptyStateCompact; a fetch error shows inline retry copy without blocking"
  artifacts:
    - apps/web/app/(all)/[workspaceSlug]/(settings)/settings/(workspace)/templates/page.tsx
    - apps/web/core/components/project-templates/list/root.tsx
    - apps/web/core/components/project-templates/list/template-row.tsx
  key_links:
    - "TWorkspaceSettingsTabs 'project-templates' -> WORKSPACE_SETTINGS + WORKSPACE_SETTINGS_ICONS (Record completeness)"
    - "routes/core.ts /settings/templates -> templates/page.tsx"
    - "useSWR(WORKSPACE_PROJECT_TEMPLATES(slug)) -> projectService.getProjectTemplates -> split by is_system"
    - "sidebar access [EUserWorkspaceRoles.ADMIN] + in-page allowPermissions([ADMIN], WORKSPACE) -> NotAuthorizedView"
---

<objective>
Deliver the thinnest visible end-to-end slice: an admin-gated "Project Templates" workspace-settings page, discoverable from the existing settings sidebar, that lists built-in (System) and custom templates in two labeled sections fetched live from the existing list endpoint. After this plan a workspace admin can navigate to the page and see their real templates; a non-admin is blocked. Row actions and the editor are stubbed/deferred to later plans — this plan proves the navigation + fetch + render path.

Purpose: This is the MVP walking path for UI-05 (discoverable management area) and the foundation every later slice hangs off. It is independent of the backend slice (Plan 01) because the default list endpoint already returns active templates.
Output: 4-touchpoint settings registration, the list page + breadcrumb header, and the two-section list components with loading/empty/error states.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-workspace-template-management/04-CONTEXT.md
@.planning/phases/04-workspace-template-management/04-RESEARCH.md
@.planning/phases/04-workspace-template-management/04-PATTERNS.md
@.planning/phases/04-workspace-template-management/04-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan produces:

- Route paths: `/:workspaceSlug/settings/templates` (list), plus the `/new` and `/:templateId/edit` route registrations (pages themselves land in Plan 03).
- `TWorkspaceSettingsTabs` union member `"project-templates"`.
- `WORKSPACE_SETTINGS["project-templates"]` config entry + placement in `GROUPED_WORKSPACE_SETTINGS[ADMINISTRATION]`.
- `WORKSPACE_SETTINGS_ICONS["project-templates"]` (lucide icon `LayoutTemplate` unless unavailable).
- Page component `TemplatesListPage` and `ProjectTemplatesSettingsHeader`.
- Components `ProjectTemplatesListRoot`, `ProjectTemplateRow`, `ProjectTemplatesListLoader`, and `components/project-templates/index.ts` barrel.
- i18n namespace `workspace_settings.settings.project_templates.*` (English source keys).
- SWR fetch reuse of `WORKSPACE_PROJECT_TEMPLATES(workspaceSlug)`.
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Register the admin-only Project Templates settings page (4 touchpoints + routes + i18n)</name>
  <files>packages/types/src/settings.ts, packages/constants/src/settings/workspace.ts, apps/web/core/components/settings/workspace/sidebar/item-icon.tsx, apps/web/app/routes/core.ts, packages/i18n/src/locales/en/workspace-settings.json</files>
  <read_first>
    - packages/types/src/settings.ts (line 13 `TWorkspaceSettingsTabs` union; add member per D-03)
    - packages/constants/src/settings/workspace.ts (`webhooks` entry ~58-64, `GROUPED_WORKSPACE_SETTINGS` ADMINISTRATION bucket ~71-79)
    - apps/web/core/components/settings/workspace/sidebar/item-icon.tsx (`WORKSPACE_SETTINGS_ICONS` Record ~13-19)
    - apps/web/app/routes/core.ts (webhooks route blocks ~277-284 inside the `(workspace)` layout)
    - packages/i18n/src/locales/en/workspace-settings.json (`webhooks`/`exports` entries ~76-110 for key structure)
    - .planning/phases/04-workspace-template-management/04-RESEARCH.md (Pattern 1, lines 197-254 — exact diff-shaped edits)
    - .planning/phases/04-workspace-template-management/04-PATTERNS.md (Four-touchpoint section, lines 417-426)
  </read_first>
  <action>
    Apply the four-touchpoint registration per D-01/D-02/D-03, using RESEARCH Pattern 1 as the exact diff source.
    (1) `settings.ts`: add `| "project-templates"` to the `TWorkspaceSettingsTabs` union. This is the compile driver that forces the two `Record<TWorkspaceSettingsTabs, ...>` maps to be completed.
    (2) `workspace.ts`: add a `"project-templates"` entry keyed `project-templates`, `i18n_label: "workspace_settings.settings.project_templates.title"`, `href: "/settings/templates"`, `access: [EUserWorkspaceRoles.ADMIN]` (admin-only, D-01/D-04), and a `highlight` using `pathname.startsWith(\`${baseUrl}/settings/templates\`)` (startsWith, NOT strict ===, because this page has /new and /edit sub-routes). Push `WORKSPACE_SETTINGS["project-templates"]` into the `ADMINISTRATION` bucket of `GROUPED_WORKSPACE_SETTINGS` (per D-01, Administration — not the Developer bucket where webhooks sits).
    (3) `item-icon.tsx`: import `LayoutTemplate` from `lucide-react` and add `"project-templates": LayoutTemplate` to `WORKSPACE_SETTINGS_ICONS`. (If `LayoutTemplate` is unavailable, choose the nearest template/layout lucide glyph — Claude's Discretion.)
    (4) `routes/core.ts`: add three routes inside the `(workspace)` layout array mirroring the webhooks blocks — `:workspaceSlug/settings/templates` -> `templates/page.tsx`; `:workspaceSlug/settings/templates/new` -> `templates/new/page.tsx`; `:workspaceSlug/settings/templates/:templateId/edit` -> `templates/[templateId]/edit/page.tsx`. (The /new and /edit page files are created in Plan 03; registering the routes now keeps the touchpoint edit coherent and typegen stable — create minimal placeholder page files if typegen requires them, but the functional editor is Plan 03.)
    (5) i18n: add a `project_templates` node under `workspace_settings.settings` in the English `workspace-settings.json` with at least `title` ("Project Templates"), `description` ("Create and manage reusable project templates for this workspace."), and `new_template` ("New template") keys per the UI-SPEC Copywriting contract. Use a NEW `project_templates` namespace (do not reuse the unrelated legacy `templates` EE node — RESEARCH assumption A5).
  </action>
  <verify>
    <automated>pnpm --filter=@plane/types check:types && pnpm --filter=@plane/constants check:types</automated>
  </verify>
  <acceptance_criteria>
    - `packages/types/src/settings.ts` `TWorkspaceSettingsTabs` includes `"project-templates"`.
    - `WORKSPACE_SETTINGS["project-templates"].access` equals `[EUserWorkspaceRoles.ADMIN]` and its `highlight` uses `startsWith`.
    - `WORKSPACE_SETTINGS_ICONS` has a `"project-templates"` entry (Record is complete — type-check passes).
    - `routes/core.ts` contains the three `/settings/templates`, `/settings/templates/new`, `/settings/templates/:templateId/edit` route registrations.
    - English `workspace-settings.json` has `workspace_settings.settings.project_templates.{title,description,new_template}`.
    - `pnpm --filter=@plane/types check:types` and `pnpm --filter=@plane/constants check:types` both pass.
  </acceptance_criteria>
  <done>The Administration group shows an admin-only "Project Templates" entry linking to /settings/templates, and both package type-checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build the admin-gated list page, breadcrumb header, and two-section list</name>
  <files>apps/web/app/(all)/[workspaceSlug]/(settings)/settings/(workspace)/templates/page.tsx, apps/web/app/(all)/[workspaceSlug]/(settings)/settings/(workspace)/templates/header.tsx, apps/web/core/components/project-templates/list/root.tsx, apps/web/core/components/project-templates/list/template-row.tsx, apps/web/core/components/project-templates/list/loader.tsx, apps/web/core/components/project-templates/index.ts</files>
  <read_first>
    - apps/web/app/(all)/[workspaceSlug]/(settings)/settings/(workspace)/webhooks/page.tsx (admin gate, SettingsContentWrapper, SettingsHeading, observer, Route.ComponentProps — the exact page shell)
    - apps/web/app/(all)/[workspaceSlug]/(settings)/settings/(workspace)/webhooks/header.tsx (breadcrumb header analog — swap `.webhooks` for `["project-templates"]`)
    - apps/web/core/components/labels/project-setting-label-list.tsx (list scaffold: Loader + EmptyStateCompact + modal-in-state, ~76-171)
    - apps/web/core/components/project-states/group-list.tsx (grouped-section container idiom, ~61-84 — iterate groups render section per group)
    - apps/web/core/components/web-hooks/webhooks-list-item.tsx (row container: bordered bg-layer row, truncated name, right control ~31-43)
    - apps/web/ce/components/projects/create/template-select.tsx (SWR + service fetch idiom ~19-67)
    - packages/types/src/project/project_templates.ts (`TProjectTemplate` shape — `is_system`, `template_type`, `payload` for counts)
    - apps/web/core/components/settings/content-wrapper.tsx (hugging prop; list uses non-hugging max-w-225)
    - .planning/phases/04-workspace-template-management/04-UI-SPEC.md (Screen 1, Copywriting, Color/Typography tokens)
    - .planning/phases/04-workspace-template-management/04-PATTERNS.md (list/page/header/root/row/loader sections, lines 69-222)
  </read_first>
  <action>
    Create the list page and its components under the paths in files_modified, composing existing primitives only (UI-SPEC Component Inventory).
    `templates/page.tsx`: `observer`-wrapped `TemplatesListPage({ params }: Route.ComponentProps)`. Read `workspaceSlug` from params; get `{ workspaceUserInfo, allowPermissions }` from `useUserPermissions`; compute `canPerformWorkspaceAdminActions = allowPermissions([EUserPermissions.ADMIN], EUserPermissionsLevel.WORKSPACE)`; if `workspaceUserInfo && !canPerformWorkspaceAdminActions` return `NotAuthorizedView section="settings" className="h-auto"` (D-04). Render `SettingsContentWrapper` (non-hugging, max-w-225) with `header={<ProjectTemplatesSettingsHeader />}`, a `PageHead` title, and a `SettingsHeading variant="h4"` with title/description from the `project_templates` i18n keys and a `Button variant="primary" size="lg"` "New template" in the `control` slot that navigates to `/${workspaceSlug}/settings/templates/new`. Render `<ProjectTemplatesListRoot workspaceSlug={workspaceSlug} />` below.
    `templates/header.tsx`: `ProjectTemplatesSettingsHeader` mirroring webhooks/header.tsx — `SettingsPageHeader` + `Breadcrumbs`/`BreadcrumbLink`, reading `WORKSPACE_SETTINGS["project-templates"]` (bracket access, hyphenated key) and `WORKSPACE_SETTINGS_ICONS["project-templates"]`.
    `list/root.tsx` (`ProjectTemplatesListRoot`): `useSWR(WORKSPACE_PROJECT_TEMPLATES(workspaceSlug), () => projectService.getProjectTemplates(workspaceSlug), { revalidateIfStale:false, revalidateOnFocus:false })`. Split `data` into System (`is_system === true`) and Custom (`is_system === false`) arrays. Render two sections per D-05: a "System templates" heading (`text-h6-medium`) + description + rows, then a "Custom templates" heading + description + rows, separated by `lg` (24px) gap. Loading (`data` undefined, no error) -> `<ProjectTemplatesListLoader />`. Custom section empty -> `EmptyStateCompact` (align="start", rootClassName="py-20") with the no-custom-templates copy + a "New template" action. Error -> inline error copy ("Couldn't load templates...") with a retry that calls `mutate()`; non-blocking. Copy strings from UI-SPEC Copywriting (add missing i18n keys under `project_templates.*`).
    `list/template-row.tsx` (`ProjectTemplateRow`): bordered `bg-layer-2 border-subtle rounded-lg px-4 py-3` row (webhooks-list-item idiom). Show template name (`text-body-sm-medium`) + a one-line description/counts caption (`text-body-xs-regular text-tertiary`) derived from `payload` section lengths (e.g. "N states · M labels · ..."). Reserve a right-aligned actions slot. In THIS plan the row renders provenance-correct placeholders only: built-in rows show muted `text-tertiary` metadata and no edit controls (provenance by heading, not tint — UI-SPEC Color); the full Edit/Duplicate/Deactivate/Reactivate action wiring is Plan 05. Do not build custom UI primitives.
    `list/loader.tsx` (`ProjectTemplatesListLoader`): `Loader` with a few `Loader.Item height="42px"` rows (labels idiom).
    `index.ts`: barrel-export the list components.
    Use only semantic Tailwind tokens (bg-surface-1 / bg-layer-1 / border-subtle / text-primary|secondary|tertiary / ring-accent-strong) per UI-SPEC Color — no raw hex, no raw palette steps. Accent reserved for the primary CTA only.
  </action>
  <verify>
    <automated>pnpm --filter=web check:types && pnpm --filter=web check:lint</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web check:types` (react-router typegen && tsc --noEmit) passes with the new routes and pages; `check:lint` adds no new warnings above the existing baseline.
    - `templates/page.tsx` returns `NotAuthorizedView` when `workspaceUserInfo && !canPerformWorkspaceAdminActions` (grep: `NotAuthorizedView` and `allowPermissions([EUserPermissions.ADMIN]`).
    - `list/root.tsx` fetches via `useSWR(WORKSPACE_PROJECT_TEMPLATES(...))` and splits by `is_system`; renders distinct "System templates" and "Custom templates" section headings.
    - Loading renders `Loader.Item`; custom-empty renders `EmptyStateCompact`; error path renders inline retry copy (grep the three branches in `root.tsx`).
    - No inline axios/fetch — all data access via `projectService` (grep: no `axios`/`fetch(` in the new files).
  </acceptance_criteria>
  <done>An admin visiting /settings/templates sees their real System and Custom templates in two labeled sections with loading/empty/error handled; non-admins are blocked; web type-check + lint pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                     |
| ----------------------------------- | --------------------------------------------------------------- |
| browser -> workspace settings route | Non-admin may navigate directly to /settings/templates          |
| SWR client <- list API              | Template names/descriptions (user-authored) rendered in the DOM |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Severity | Disposition | Mitigation Plan                                                                                                                                                                    |
| --------- | ---------------------- | ----------------------------------------------- | -------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-05   | Elevation of Privilege | list page direct-URL access                     | high     | mitigate    | In-page `allowPermissions([ADMIN], WORKSPACE)` guard -> `NotAuthorizedView` (D-04) plus sidebar `access:[ADMIN]`; backend list still enforces ADMIN/MEMBER (defense in depth, V4). |
| T-04-06   | Information Disclosure | rendering template name/description             | medium   | mitigate    | React auto-escapes; do NOT use `dangerouslySetInnerHTML` for template-supplied strings (V5/XSS).                                                                                   |
| T-04-07   | Tampering              | crafted/oversized template strings in list rows | low      | accept      | Read-only render; truncated with `truncate`; backend enforces max_length.                                                                                                          |

</threat_model>

<verification>
- `pnpm --filter=@plane/types check:types` and `pnpm --filter=@plane/constants check:types` pass (registration).
- `pnpm --filter=web check:types` passes (react-router typegen regenerates `./+types/page` for the new routes).
- `pnpm --filter=web check:lint` introduces no new warnings above the existing `--max-warnings` baseline.
- Manual confirmation of admin gating + rendering is covered by the end-of-phase human-verify checkpoint (Plan 05).
</verification>

<success_criteria>

- Discoverable admin-only "Project Templates" settings page exists and lists System + Custom templates end-to-end.
- Non-admins are blocked in-page; loading/empty/error states are handled.
- All type-checks and lint pass.
  </success_criteria>

<output>
Create `.planning/phases/04-workspace-template-management/04-02-SUMMARY.md` when done.
</output>
