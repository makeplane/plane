---
phase: 03-create-modal-template-selection
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/web/ce/components/projects/create/template-select.tsx
  - apps/web/core/components/project/create/header.tsx
autonomous: true
requirements:
  - UI-04
  - VER-05
requirements_addressed:
  - UI-04
  - VER-05
must_haves:
  truths:
    - "D-13/D-14/D-15/D-16/D-17: loading, empty, and error states keep No template available and never block normal no-template creation."
    - "D-04 and UI-SPEC: the compact cover button remains the mobile and desktop placement, with dropdown sizing constrained to the viewport."
    - "VER-05: targeted frontend type and lint checks pass for template types, service methods, store/form payloads, and create modal components."
    - "AGENTS.md testing requirement is addressed by an explicit no-test-harness exception for the affected frontend packages, verified by package manifest assertions."
  artifacts:
    - "apps/web/ce/components/projects/create/template-select.tsx"
    - "apps/web/ce/components/projects/create/root.tsx"
    - "apps/web/core/components/project/create/header.tsx"
    - "apps/web/core/components/project/create-project-modal.tsx"
  key_links:
    - "Selector loading/error state -> No template option -> local selectedTemplate null"
    - "Retry action -> SWR mutate -> ProjectService.getProjectTemplates"
    - "No selection submit -> TProjectCreatePayload without template_id"
---

## Phase Goal

**As a** workspace member who can create Projects, **I want to** select and preview a Project Template in the existing create Project modal, **so that** the create request can apply the chosen template without changing the normal no-template flow.

<objective>
Complete Phase 03 by hardening the selector state matrix, accessibility, mobile fit, source assertions, and frontend verification for the create modal template selection flow.

Purpose: close UI-04 and VER-05 while preserving all locked decisions D-01 through D-22 from the Phase 03 context.
Output: non-blocking loading/empty/error/retry behavior, accessible compact dropdown polish, targeted type/lint verification, and source coverage audit.
</objective>

<execution_context>
@/root/.codex/gsd-core/workflows/execute-plan.md
@/root/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-create-modal-template-selection/03-CONTEXT.md
@.planning/phases/03-create-modal-template-selection/03-UI-SPEC.md
@.planning/phases/03-create-modal-template-selection/03-RESEARCH.md
@.planning/phases/03-create-modal-template-selection/03-PATTERNS.md
@.planning/phases/03-create-modal-template-selection/03-01-SUMMARY.md
@apps/web/ce/components/projects/create/template-select.tsx
@apps/web/ce/components/projects/create/root.tsx
@apps/web/core/components/project/create/header.tsx
@apps/web/core/components/project/create-project-modal.tsx
@apps/web/core/services/project/project.service.ts
@apps/web/core/store/project/project.store.ts
@packages/types/src/project/project_templates.ts
@packages/types/src/project/projects.ts
@packages/types/src/project/index.ts
@packages/constants/src/fetch-keys.ts
</context>

## Schema Push Detection

No schema push is required. This plan must not modify Django models, migrations, serializers, ORM files, or database schema artifacts; backend references are verification context only.

## Artifacts This Phase Produces

- Non-blocking loading row with `Loading...` in `ProjectTemplateSelect`.
- Empty state row with `No templates available` in `ProjectTemplateSelect`.
- Inline error row with `Could not load templates` and a `Retry` action wired to SWR retry.
- Accessibility attributes for the selector button, including selected-template accessible label.
- Source assertions and summary evidence proving type safety and locked-decision coverage for Phase 03.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add non-blocking loading, empty, error, and retry states</name>
  <read_first>
    @.planning/phases/03-create-modal-template-selection/03-CONTEXT.md
    @.planning/phases/03-create-modal-template-selection/03-UI-SPEC.md
    @.planning/phases/03-create-modal-template-selection/03-RESEARCH.md
    @.planning/phases/03-create-modal-template-selection/03-PATTERNS.md
    @.planning/phases/03-create-modal-template-selection/03-01-SUMMARY.md
    @apps/web/ce/components/projects/create/template-select.tsx
    @apps/web/ce/components/projects/create/root.tsx
  </read_first>
  <files>apps/web/ce/components/projects/create/template-select.tsx</files>
  <behavior>
    - Loading dropdown state shows No template first and Loading... as inline state text.
    - Empty dropdown state shows No template first and No templates available.
    - Error dropdown state shows No template first, Could not load templates, and Retry.
    - Retry calls the SWR mutate/refetch path and does not show a toast.
    - Submitting with no selected template during loading, empty, or error state still creates through the no-template payload path.
  </behavior>
  <action>Complete the UI-04 state matrix per D-13 through D-17. Keep `No template` as the first selectable option in loading, empty, error, and result states. Render `Loading...`, `No templates available`, `Could not load templates`, and `Retry` exactly per UI-SPEC copy. Wire Retry to the local SWR refetch/mutate path. Do not call `setToast` from `template-select.tsx`; catalog failures stay inline. Ensure `onChange(null)` is available from every dropdown state and that root submit logic still omits the template identifier when selectedTemplate is null.</action>
  <verify>
    <automated>pnpm turbo run check:types --filter=web</automated>
    <automated>pnpm turbo run check:lint --filter=web</automated>
    <automated>rg -n "Loading\\.\\.\\.|No templates available|Could not load templates|Retry|mutate|onChange\\(null\\)" apps/web/ce/components/projects/create/template-select.tsx</automated>
    <automated>test "$(rg -n "setToast|toast" apps/web/ce/components/projects/create/template-select.tsx | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    Loading, empty, and error states are visible inside the dropdown, Retry refetches the catalog, and no-template Project creation remains reachable when the catalog is unavailable.
  </acceptance_criteria>
  <done>UI-04 state handling is implemented without global toasts or create-flow blocking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce UI-SPEC accessibility, mobile fit, and visual guardrails</name>
  <read_first>
    @.planning/phases/03-create-modal-template-selection/03-CONTEXT.md
    @.planning/phases/03-create-modal-template-selection/03-UI-SPEC.md
    @.planning/phases/03-create-modal-template-selection/03-PATTERNS.md
    @apps/web/ce/components/projects/create/template-select.tsx
    @apps/web/core/components/project/create/header.tsx
    @apps/web/core/components/project/create-project-modal.tsx
  </read_first>
  <files>apps/web/ce/components/projects/create/template-select.tsx, apps/web/core/components/project/create/header.tsx</files>
  <behavior>
    - The button is `type="button"` with an accessible name for no selection and selected template.
    - Mobile and desktop both use the compact cover button placement; the dropdown width and max-height fit the viewport.
    - Text truncates inside fixed max widths and does not introduce badges, counts, group labels, provenance labels, banners, or selected-template rows.
    - Semantic Plane tokens and 4px spacing-grid classes are used for selector surfaces, rows, borders, and inline state text.
  </behavior>
  <action>Polish the selector against the UI-SPEC. Add `aria-label` values matching D-02/D-09: `Select project template` with no selection and `Selected project template: {name}` when selected. Confirm keyboard behavior follows Headless UI Combobox conventions and Escape closes without changing selection. Keep button max widths around `max-w-[160px]` desktop and `max-w-[140px]` mobile, keep row spacing on `px-2 py-2`, keep radius at `rounded-md` or smaller, and use Plane semantic surface, border, primary-text, secondary-text, and muted-state tokens rather than hardcoded colors. Preserve the existing header top-left cover slot and modal feature-selection flow.</action>
  <verify>
    <automated>pnpm turbo run check:types --filter=web</automated>
    <automated>pnpm turbo run check:lint --filter=web</automated>
    <automated>rg -n "aria-label|Select project template|Selected project template|max-w-\\[160px\\]|max-w-\\[140px\\]|rounded-md|bg-surface-1|text-secondary|border-subtle" apps/web/ce/components/projects/create/template-select.tsx apps/web/core/components/project/create/header.tsx</automated>
    <automated>test "$(rg -n "badge|group|count|is_system|template_type|payload|No description" apps/web/ce/components/projects/create/template-select.tsx | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    Selector layout, copy, accessible labels, and option rendering match the UI-SPEC and locked decisions across desktop and mobile constraints.
  </acceptance_criteria>
  <done>The selector is polished enough for the existing create modal and avoids all forbidden Phase 03 visual elements.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 03 frontend verification and coverage audit</name>
  <read_first>
    @.planning/ROADMAP.md
    @.planning/REQUIREMENTS.md
    @.planning/phases/03-create-modal-template-selection/03-CONTEXT.md
    @.planning/phases/03-create-modal-template-selection/03-UI-SPEC.md
    @.planning/phases/03-create-modal-template-selection/03-RESEARCH.md
    @.planning/phases/03-create-modal-template-selection/03-PATTERNS.md
    @.planning/phases/03-create-modal-template-selection/03-01-SUMMARY.md
    @apps/web/ce/components/projects/create/template-select.tsx
    @apps/web/ce/components/projects/create/root.tsx
    @apps/web/core/components/project/create/header.tsx
    @apps/web/core/components/project/create-project-modal.tsx
    @apps/web/core/services/project/project.service.ts
    @apps/web/core/store/project/project.store.ts
    @packages/types/src/project/project_templates.ts
    @packages/types/src/project/projects.ts
    @packages/types/src/project/index.ts
    @packages/constants/src/fetch-keys.ts
  </read_first>
  <files>.planning/phases/03-create-modal-template-selection/03-02-SUMMARY.md</files>
  <action>Run final targeted frontend checks for VER-05 and record the results in `03-02-SUMMARY.md`. Confirm source coverage for all Phase 03 requirements, all D-01 through D-22 locked decisions, UI-SPEC copy/spacing/token constraints, and RESEARCH constraints. Address the AGENTS.md unit-test requirement with a documented no-test-harness exception: verify the affected frontend package manifests do not expose `test` or `test:unit` scripts, then rely on targeted type/lint/format checks and source assertions. If a relevant test script exists by execution time, add focused unit coverage before completing this task instead of using the exception. Confirm no package additions and no backend schema/database modifications. If type or lint checks expose unrelated pre-existing failures, record exact command output and narrow source assertions that prove the Phase 03 files are correct.</action>
  <verify>
    <automated>pnpm turbo run check:types --filter=@plane/types --filter=@plane/constants --filter=web</automated>
    <automated>pnpm turbo run check:lint --filter=@plane/types --filter=@plane/constants --filter=web</automated>
    <automated>pnpm turbo run check:format --filter=@plane/types --filter=@plane/constants --filter=web</automated>
    <automated>test "$(rg -n '"test(:unit)?"\\s*:' apps/web/package.json packages/types/package.json packages/constants/package.json | wc -l)" -eq 0</automated>
    <automated>git diff --name-only | rg "^(apps/web|packages/types|packages/constants)/" && test "$(git diff --name-only | rg "^(apps/api|.*migrations|.*schema)" | wc -l)" -eq 0</automated>
    <automated>rg -n "TProjectTemplate|TProjectCreatePayload|WORKSPACE_PROJECT_TEMPLATES|getProjectTemplates|selectedTemplate|Loading\\.\\.\\.|No templates available|Could not load templates|Retry|aria-label" packages/types/src/project packages/constants/src apps/web/core/services/project apps/web/core/store/project apps/web/ce/components/projects/create apps/web/core/components/project/create</automated>
  </verify>
  <acceptance_criteria>
    VER-05 is evidenced by passing targeted type/lint/format checks or by precise documentation of unrelated failures plus passing source assertions for all changed Phase 03 files. The summary documents the no-test-harness exception only if the manifest assertion proves no affected frontend unit-test script exists. No backend schema files are touched.
  </acceptance_criteria>
  <done>Phase 03 is verified and the summary contains command results, the frontend test-harness finding, and a source coverage audit.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                              |
| ------------------------ | ------------------------------------------------------------------------ |
| Browser -> Plane API     | Catalog retry and create submit cross into authenticated backend routes. |
| API error -> dropdown UI | Permission/network failures are rendered in the selector.                |
| API template text -> DOM | Template names/descriptions are displayed in React.                      |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Severity | Disposition | Mitigation Plan                                                                                                                                            |
| --------- | ---------------------- | -------------------------------------- | -------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-05   | Denial of Service      | `ProjectTemplateSelect` catalog outage | medium   | mitigate    | Keep `No template` selectable and make submit omit the template identifier when no selection exists, so catalog outage does not block Project creation.    |
| T-03-06   | Information Disclosure | Inline error state                     | medium   | mitigate    | Use only generic `Could not load templates` copy and Retry; do not render response bodies, roles, permission details, or unavailable template identifiers. |
| T-03-07   | Cross-Site Scripting   | Template option text                   | medium   | mitigate    | Render names/descriptions as plain React children and do not introduce HTML parsing, markdown rendering, or dangerously-set HTML APIs.                     |
| T-03-08   | Repudiation            | Selection state before submit          | low      | accept      | The selector mutates no server state before submit; backend Project creation audit remains authoritative after create.                                     |
| T-03-SC   | Tampering              | npm/pip/cargo installs                 | high     | mitigate    | No package-manager install tasks are planned; existing dependencies only, matching the Package Legitimacy Audit.                                           |

</threat_model>

## Multi-Source Coverage Audit

| Source   | ID                  | Feature/Requirement                                                                                                    | Plan         | Status  | Notes                                                                       |
| -------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------ | ------- | --------------------------------------------------------------------------- |
| GOAL     | -                   | Select and preview Project Templates in the existing create Project modal, then submit template_id with create request | 03-01, 03-02 | COVERED | Happy path plus fallback states complete the vertical slice.                |
| REQ      | CAT-01              | Available templates shown inside existing create Project modal/form                                                    | 03-01        | COVERED | Existing header slot renders selector.                                      |
| REQ      | CAT-06              | Useful summary before selection                                                                                        | 03-01        | COVERED | D-06 locks description as the summary; rows show optional description only. |
| REQ      | PERM-02             | Members who can create Projects can list/select templates                                                              | 03-01        | COVERED | Selector uses Phase 01 member-allowed endpoint via ProjectService.          |
| REQ      | UI-01               | Existing ProjectTemplateSelect UI implemented in header area                                                           | 03-01        | COVERED | Stub becomes compact cover button/dropdown.                                 |
| REQ      | UI-02               | Selection updates form state and sends template_id                                                                     | 03-01        | COVERED | Local state merges submit payload only when selected.                       |
| REQ      | UI-03               | Selected template visually clear before submit                                                                         | 03-01        | COVERED | Cover button label changes to selected template name.                       |
| REQ      | UI-04               | Loading, empty, error states do not block no-template create                                                           | 03-02        | COVERED | No template remains available in every state.                               |
| REQ      | VER-05              | Frontend type safety for types, services, store/form payloads, modal components                                        | 03-02        | COVERED | Final targeted type/lint/format checks.                                     |
| RESEARCH | service-layer-fetch | Fetch templates through ProjectService, not inline fetch                                                               | 03-01        | COVERED | `getProjectTemplates` added and consumed by selector.                       |
| RESEARCH | no-new-packages     | Use existing Plane UI/Headless UI/SWR dependencies only                                                                | 03-01, 03-02 | COVERED | No install tasks; threat register keeps package gate closed.                |
| RESEARCH | local-state         | Keep selected template in CreateProjectForm, not MobX                                                                  | 03-01        | COVERED | Store typing only; no selector observables.                                 |
| RESEARCH | state-matrix        | Loading, empty, error, retry, and no-template fallback                                                                 | 03-02        | COVERED | UI-04 tasks implement every state.                                          |
| RESEARCH | xss                 | Render template description as plain React text                                                                        | 03-01, 03-02 | COVERED | Threat mitigations and source guardrails forbid HTML rendering.             |
| CONTEXT  | D-01                | Compact cover-header selector in existing stub location                                                                | 03-01        | COVERED | Header slot preserved.                                                      |
| CONTEXT  | D-02                | Icon plus text label, Template before selection and name after                                                         | 03-01, 03-02 | COVERED | Button contract and accessibility polish.                                   |
| CONTEXT  | D-03                | Simple searchable dropdown using existing patterns                                                                     | 03-01        | COVERED | Combobox/SWR service-backed selector.                                       |
| CONTEXT  | D-04                | Mobile keeps compact cover button; dropdown fits viewport                                                              | 03-02        | COVERED | Mobile sizing guardrails.                                                   |
| CONTEXT  | D-05                | Options show name and short description only                                                                           | 03-01        | COVERED | Option rendering guardrails.                                                |
| CONTEXT  | D-06                | Description is CAT-06 summary; counts not required                                                                     | 03-01        | COVERED | Rows render descriptions, no counts.                                        |
| CONTEXT  | D-07                | No built-in/custom badges or groups                                                                                    | 03-01, 03-02 | COVERED | Negative source assertions.                                                 |
| CONTEXT  | D-08                | No description fallback copy                                                                                           | 03-01, 03-02 | COVERED | Optional description element only.                                          |
| CONTEXT  | D-09                | Selected state only through cover button label                                                                         | 03-01        | COVERED | No separate banner/row.                                                     |
| CONTEXT  | D-10                | No template option clears selection                                                                                    | 03-01, 03-02 | COVERED | `onChange(null)` path.                                                      |
| CONTEXT  | D-11                | No warning when changing templates                                                                                     | 03-01        | COVERED | Selection replaces local state only.                                        |
| CONTEXT  | D-12                | Newly opened modal resets to no-template                                                                               | 03-01        | COVERED | Explicit modal-open reset.                                                  |
| CONTEXT  | D-13                | Loading keeps selector clickable and allows no-template submit                                                         | 03-02        | COVERED | Loading row plus null selection submit path.                                |
| CONTEXT  | D-14                | Empty list keeps No template and empty text                                                                            | 03-02        | COVERED | Empty state row.                                                            |
| CONTEXT  | D-15                | API failure inline error, no toast, No template remains                                                                | 03-02        | COVERED | Error row and no toast assertion.                                           |
| CONTEXT  | D-16                | Retry action in dropdown error state                                                                                   | 03-02        | COVERED | Retry wired to SWR mutate.                                                  |
| CONTEXT  | D-17                | Loading/empty/error do not block no-template creation                                                                  | 03-02        | COVERED | State matrix and submit assertions.                                         |
| CONTEXT  | D-18                | Ignore existing templateId prop                                                                                        | 03-01        | COVERED | Modal opens in local null state.                                            |
| CONTEXT  | D-19                | Omit template_id when no selection                                                                                     | 03-01, 03-02 | COVERED | Typed spread only when selected.                                            |
| CONTEXT  | D-20                | Selection local to CreateProjectForm                                                                                   | 03-01        | COVERED | React state owner; no MobX observable.                                      |
| CONTEXT  | D-21                | Merge template_id only when selected                                                                                   | 03-01        | COVERED | Submit-time payload merge.                                                  |
| CONTEXT  | D-22                | Existing success toast and feature-selection step unchanged                                                            | 03-01, 03-02 | COVERED | Root/modal behavior preserved.                                              |

<verification>
Overall Phase 03 checks:

```bash
pnpm turbo run check:types --filter=@plane/types --filter=@plane/constants --filter=web
pnpm turbo run check:lint --filter=@plane/types --filter=@plane/constants --filter=web
pnpm turbo run check:format --filter=@plane/types --filter=@plane/constants --filter=web
git diff --name-only | rg "^(apps/web|packages/types|packages/constants)/"
test "$(git diff --name-only | rg "^(apps/api|.*migrations|.*schema)" | wc -l)" -eq 0
```

</verification>

<success_criteria>
UI-04 and VER-05 are complete: the selector handles loading, empty, error, and retry states inside the dropdown without blocking no-template Project creation, and targeted frontend checks prove type safety across template types, service methods, store/form payloads, and create modal components.
</success_criteria>

<output>
Create `.planning/phases/03-create-modal-template-selection/03-02-SUMMARY.md` when done.
</output>
