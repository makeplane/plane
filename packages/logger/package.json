{
  "name": "@plane/logger",
  "version": "2.3.0",
  "private": true,
  "description": "Logger shared across multiple apps internally",
  "license": "AGPL-3.0",
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "check:lint": "eslint . --cache --cache-location node_modules/.cache/eslint/ --max-warnings=2",
    "check:types": "tsc --noEmit",
    "check:format": "oxfmt . --check",
    "fix:lint": "eslint . --cache --cache-location node_modules/.cache/eslint/ --fix --max-warnings=2",
    "fix:format": "oxfmt . --write",
    "clean": "rm -rf .turbo && rm -rf .next && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "express-winston": "^4.2.0",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@plane/typescript-config": "workspace:*",
    "@types/express": "catalog:",
    "@types/node": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  },
  "peerDependencies": {
    "@types/express": ">=4"
  }
}
