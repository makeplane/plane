# Auto-generated by scripts/build-shb-images.sh — do not edit manually.
# Version: shb_v1.2.0
# Transfer this file to the production server alongside dist/ and scripts/deploy-shb.sh
#
# Usage on server:
#   docker compose -f docker-compose.yml -f docker-compose.shb.yml up -d --no-build
#
# Rollback:
#   docker compose -f docker-compose.yml up -d

services:
  web:
    image: makeplane/plane-frontend:shb_v1.2.0

  admin:
    image: makeplane/plane-admin:shb_v1.2.0

  space:
    image: makeplane/plane-space:shb_v1.2.0

  live:
    image: makeplane/plane-live:shb_v1.2.0

  api:
    image: makeplane/plane-backend:shb_v1.2.0

  worker:
    image: makeplane/plane-backend:shb_v1.2.0

  beat-worker:
    image: makeplane/plane-backend:shb_v1.2.0

  migrator:
    image: makeplane/plane-backend:shb_v1.2.0

  proxy:
    image: makeplane/plane-proxy:shb_v1.2.0
