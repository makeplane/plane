"use client";

type Props = {
  className?: string;
};

/**
 * Upgrade banner disabled - all features enabled by default
 */
export const BulkOperationsUpgradeBanner: React.FC<Props> = (props) => {
  // Banner disabled - bulk operations available to all users
  return null;
};
