# Programs Feature

This folder is reserved for future program-specific customizations.

Plane's existing program behavior is still implemented through the established project/program modules in `core/` and `ce/components/projects/`. Keep that architecture unchanged unless a future refactor can isolate a self-contained program feature without changing routes or store behavior.
