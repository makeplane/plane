// Intercom configuration disabled for government deployment
// Original allowed enabling Intercom chat support

type TIntercomConfig = {
  isTelemetryEnabled: boolean;
};

export function IntercomConfig(_props: TIntercomConfig) {
  return null;
}
