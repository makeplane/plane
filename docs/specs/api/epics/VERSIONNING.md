# VERSIONNING — api/epics

| Version | Date | Type | Description | Fichiers touchés |
|---------|------|------|-------------|------------------|
| 1.0.1 | 2026-07-09 | fix | Revue sécurité adversariale (12 findings confirmés). Backend : conversion en epic refusée si l'issue est en cycle/module (guard serializer) + `filter_epics` défensif sur listes cycle/module ; recherche exclut les epics des pickers cycle/module/sous-item ; `is_epic` exposé en lecture par `IssueDetailSerializer` (+`select_related('type')`) ; `estimate_point` strippé des mutations d'epic. Web : fix régression WB-1 (modale d'édition épinglée au store ISSUES + try/catch) ; second peek projet pour les enfants ; peek epic masque estimate/cycle/module/parent ; Subscribe threadé ; garde route `/epics` ; tooltip i18n `epic.count_tooltip` (19 locales). 89 tests pytest verts, check:types EXIT=0. | apps/api/plane/app/serializers/issue.py, app/views/{cycle,module}/issue.py, app/views/search/issue.py, app/views/epic/base.py, app/views/issue/base.py, tests/contract/app/test_epic.py ; apps/web/core/components/issues/{peek-overview/{header,view,properties}.tsx,issue-modal/base.tsx,issue-layouts/roots/project-epics-layout-root.tsx}, app/(all)/…/epics/(list)/{page,header}.tsx, packages/i18n/src/locales/*/work-item.json |
| 1.0.0 | 2026-07-08 | feat | Epics (feature Pro) réimplémentés en CE. Backend : routes internes `/epics/…` (CRUD + enfants + `state_distribution` + links/commentaires/réactions/historique/subscribe/pages/attachments) via viewsets paramétrés `type__is_epic=True` — aucun nouveau modèle, aucune migration. Guards transverses : `filter_epics` (exclusion listes standards), cycles/modules/archive bloqués (400), `parent` strippé sur les epics, `is_epic` verrouillé au POST des types (2 surfaces), type Epic non supprimable. v1 expose `is_epic` en lecture (non-breaking). Web : stores `ProjectEpics`/`ProjectEpicsFilter` réels (serviceType EPICS, clé localStorage distincte), route+page `/projects/:id/epics`, sidebar gated `is_issue_type_enabled`, `CreateUpdateEpicModal` (type forcé, parent absent, custom properties du type epic intégrées), `EpicQuickActions` sans Archive, empty state réel, fix peek (`issueServiceType` threadée root→view→widgets), `ISSUE_DISPLAY_FILTERS_BY_PAGE.epics`, i18n 3 clés × 19 locales. **53 tests pytest verts** (BDD Docker), contrat vérifié E2E sur l'API vivante, `check:types` EXIT=0. | apps/api/plane/app/views/epic/base.py, plane/app/urls/epic.py, plane/utils/issue_type.py, apps/api/tests/contract/app/test_epic.py, tests/contract/app/test_epic_v1.py ; apps/web/ce/store/issue/epic/{issue,filter}.store.ts, core/store/issue/project-issues/project-issues-filter.store.ts, app/(all)/projects/[projectId]/epics/*, ce/components/epics/* |

> Table mise à jour par @update-writer-after-implement après chaque implémentation.
